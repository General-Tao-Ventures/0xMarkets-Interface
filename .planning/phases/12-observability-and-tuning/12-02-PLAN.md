---
phase: 12-observability-and-tuning
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - ../order-execution-keeper-service/src/utils/healthState.ts
  - ../order-execution-keeper-service/src/server/httpServer.ts
  - ../order-execution-keeper-service/src/index.ts
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Health endpoint returns 200 during idle periods when no user operations are occurring"
    - "Health endpoint returns 200 during startup grace period before first scan completes"
    - "Health endpoint returns 503 when scan cycle heartbeat is stale (>90s)"
    - "Health endpoint reports p50, p95, and sampleCount in latency object"
    - "Health endpoint includes oracleConnected and wsConnected subsystem status"
    - "BetterStack does not fire false-positive alerts during 5+ minutes of keeper inactivity"
  artifacts:
    - path: "../order-execution-keeper-service/src/utils/healthState.ts"
      provides: "Multi-signal heartbeat liveness model with latency tracker integration"
      exports: ["healthState", "recordHeartbeat", "recordExecution", "setOracleStatus", "setWsStatus", "latencyTracker"]
    - path: "../order-execution-keeper-service/src/server/httpServer.ts"
      provides: "Heartbeat-based /health endpoint with latency percentiles"
      contains: "HEARTBEAT_THRESHOLD_MS"
    - path: "../order-execution-keeper-service/src/index.ts"
      provides: "Wiring: recordHeartbeat replaces recordScanCycle, recordExecution passes latencyMs"
      contains: "recordHeartbeat"
  key_links:
    - from: "../order-execution-keeper-service/src/utils/healthState.ts"
      to: "../order-execution-keeper-service/src/utils/latencyTracker.ts"
      via: "import { LatencyTracker }"
      pattern: "import.*LatencyTracker.*latencyTracker"
    - from: "../order-execution-keeper-service/src/server/httpServer.ts"
      to: "../order-execution-keeper-service/src/utils/healthState.ts"
      via: "import latencyTracker + healthState"
      pattern: "import.*latencyTracker.*healthState"
    - from: "../order-execution-keeper-service/src/index.ts"
      to: "../order-execution-keeper-service/src/utils/healthState.ts"
      via: "import recordHeartbeat + recordExecution"
      pattern: "import.*recordHeartbeat.*healthState"
---

<objective>
Refactor the health monitoring system from execution-timestamp-based liveness to a multi-signal heartbeat model, and wire LatencyTracker into the /health endpoint response.

Purpose: Eliminates false-positive BetterStack alerts during idle periods (INFRA-02) and exposes p50/p95 latency percentiles for monitoring (INFRA-03).
Output: A /health endpoint that correctly reports the keeper as healthy when it is alive and scanning (regardless of user activity), and includes execution latency percentiles.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-observability-and-tuning/12-RESEARCH.md
@.planning/phases/12-observability-and-tuning/12-01-SUMMARY.md

# Source files to modify
@../order-execution-keeper-service/src/utils/healthState.ts
@../order-execution-keeper-service/src/server/httpServer.ts
@../order-execution-keeper-service/src/index.ts
@../order-execution-keeper-service/src/server/controllers/healthController.ts
@../order-execution-keeper-service/src/server/routes/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor healthState.ts to multi-signal heartbeat model with latency tracking</name>
  <files>../order-execution-keeper-service/src/utils/healthState.ts</files>
  <action>
    Refactor `src/utils/healthState.ts` to replace the execution-timestamp-based liveness model with a multi-signal heartbeat model:

    1. Import `LatencyTracker` from `./latencyTracker.js` and export a singleton instance: `export const latencyTracker = new LatencyTracker(200);`

    2. Update the `healthState` object:
       - Add `lastHeartbeatTime: null as Date | null` — primary liveness signal, updated by scan cycle
       - Keep `lastExecutionTime` (informational, NOT used for liveness)
       - Keep `executionCounts`, `oracleConnected`, `wsConnected`
       - Remove `lastEventTime` (dead code, never called anywhere — see Research Pitfall 3)

    3. Rename `recordScanCycle()` to `recordHeartbeat()`:
       - Updates `healthState.lastHeartbeatTime = new Date()` (NOT lastExecutionTime)
       - This is the semantic fix: scan cycle is a heartbeat signal, not an execution

    4. Update `recordExecution(type, latencyMs)`:
       - Add `latencyMs: number` parameter
       - Keep updating `healthState.lastExecutionTime = new Date()` and incrementing counts
       - Add: `latencyTracker.record(latencyMs)` to feed the percentile tracker

    5. Remove `recordEvent()` function entirely (dead code per Research Pitfall 3)

    6. Keep `setOracleStatus()` and `setWsStatus()` unchanged.

    Important: The function signature change for `recordExecution` (adding latencyMs param) must match the call site update in Task 2.
  </action>
  <verify>
    <automated>cd /Users/ken/Projects/0xM/order-execution-keeper-service && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify healthState.ts exports recordHeartbeat (not recordScanCycle), recordExecution accepts latencyMs, latencyTracker is exported, recordEvent and lastEventTime are removed</manual>
  </verify>
  <done>healthState.ts uses multi-signal heartbeat model: lastHeartbeatTime for liveness (separate from lastExecutionTime), latencyTracker wired in, dead code (recordEvent/lastEventTime) removed</done>
</task>

<task type="auto">
  <name>Task 2: Refactor /health endpoint and wire heartbeat + latency into index.ts</name>
  <files>
    ../order-execution-keeper-service/src/server/httpServer.ts
    ../order-execution-keeper-service/src/index.ts
  </files>
  <action>
    **httpServer.ts changes:**

    1. Import `latencyTracker` from `../utils/healthState.js` (alongside existing healthState import)

    2. Replace `HEALTH_THRESHOLD_MS = 2 * 60 * 1000` with:
       - `const HEARTBEAT_THRESHOLD_MS = 90_000;` (3x the 30s scan interval — tolerates 1 missed scan, catches real failures)
       - `const STARTUP_GRACE_MS = 90_000;` (grace period for startup before first scan)

    3. Refactor the `/health` route handler:
       - Compute `uptime = now - healthState.startedAt.getTime()`
       - `inStartupGrace = uptime < STARTUP_GRACE_MS`
       - `heartbeatAlive = lastHeartbeatTime !== null && (now - lastHeartbeatTime.getTime()) < HEARTBEAT_THRESHOLD_MS`
       - `isHealthy = inStartupGrace || heartbeatAlive`
       - Get percentiles: `const percentiles = latencyTracker.getPercentiles()`
       - Return JSON with: `status`, `service`, `uptime`, `lastHeartbeatTime` (ISO string or null), `lastExecutionTime` (ISO string or null), `oracleConnected`, `wsConnected`, `executionCounts`, `latency: { p50, p95, sampleCount }`
       - Status code: 200 if healthy, 503 if unhealthy

    **index.ts changes:**

    1. Update import: replace `recordScanCycle` with `recordHeartbeat` in the import from `./utils/healthState.js`

    2. In `scanAndEnqueue()`: replace `recordScanCycle()` call (line 62) with `recordHeartbeat()`

    3. In `drainQueue()`: update `recordExecution(item.type)` call (line 107) to pass latencyMs:
       - Move `const latencyMs = Date.now() - item.detectedAt;` BEFORE `recordExecution`
       - Call `recordExecution(item.type, latencyMs)`
       - Keep the existing log.info line with latencyMs (already there)

    Do NOT modify `/api/health` in healthController.ts — leave it as a lightweight "process alive" check. The `/health` endpoint is the BetterStack-facing liveness check.
  </action>
  <verify>
    <automated>cd /Users/ken/Projects/0xM/order-execution-keeper-service && npx tsc --noEmit && pnpm test</automated>
    <manual>Verify: (1) /health uses heartbeat threshold not execution timestamp, (2) startup grace period prevents 503 on fresh start, (3) latency percentiles appear in /health response, (4) index.ts calls recordHeartbeat not recordScanCycle</manual>
  </verify>
  <done>/health endpoint returns 200 during idle periods (heartbeat from 30s scan cycle), returns 200 during startup grace, returns 503 only on genuine scan cycle failure (>90s stale), includes p50/p95/sampleCount in response JSON. No false-positive BetterStack alerts during keeper inactivity.</done>
</task>

</tasks>

<verification>
1. `cd /Users/ken/Projects/0xM/order-execution-keeper-service && npx tsc --noEmit` — zero TypeScript errors
2. `cd /Users/ken/Projects/0xM/order-execution-keeper-service && pnpm test` — all tests pass (including LatencyTracker from 12-01)
3. Grep verification:
   - `grep -r "recordScanCycle" src/` returns NO results (fully renamed)
   - `grep -r "recordHeartbeat" src/` returns results in healthState.ts and index.ts
   - `grep -r "lastEventTime\|recordEvent" src/` returns NO results (dead code removed)
   - `grep "HEARTBEAT_THRESHOLD_MS" src/server/httpServer.ts` returns the 90s constant
   - `grep "latencyTracker" src/utils/healthState.ts src/server/httpServer.ts` confirms wiring
</verification>

<success_criteria>
- TypeScript compiles without errors
- All vitest tests pass
- /health endpoint uses heartbeat liveness (not execution timestamp)
- /health response includes latency.p50, latency.p95, latency.sampleCount
- /health returns 200 during startup grace period
- /health returns 200 when scan cycle is recent but no executions have occurred
- recordScanCycle is fully renamed to recordHeartbeat across the codebase
- Dead code (recordEvent, lastEventTime) is removed
</success_criteria>

<output>
After completion, create `.planning/phases/12-observability-and-tuning/12-02-SUMMARY.md`
</output>
