---
phase: 05-liquidity-swaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Pools/Pools.tsx
  - src/components/GmList/GmList.tsx
  - src/components/GmList/GmListItem.tsx
autonomous: false
requirements: [LIQ-02]

must_haves:
  truths:
    - "Pools page has 'All Pools' and 'My Pools' tabs"
    - "All Pools tab shows all 6 market pools with stats"
    - "My Pools tab shows only pools where user has a GM token balance"
    - "My Pools tab displays PnL (how much user gained or lost since depositing)"
    - "Pool rows show utilization percentage"
  artifacts:
    - path: "src/pages/Pools/Pools.tsx"
      provides: "Pools page with All Pools / My Pools tab switching"
      contains: "My Pools"
    - path: "src/components/GmList/GmList.tsx"
      provides: "GmList component with tab filtering and utilization display"
      contains: "My Pools"
    - path: "src/components/GmList/GmListItem.tsx"
      provides: "Pool row with utilization percentage and PnL display"
      contains: "utilization"
  key_links:
    - from: "src/pages/Pools/Pools.tsx"
      to: "src/components/GmList/GmList.tsx"
      via: "activeTab prop controlling pool filtering"
      pattern: "activeTab|poolsTab"
    - from: "src/components/GmList/GmList.tsx"
      to: "useUserEarnings"
      via: "user earnings data for PnL in My Pools tab"
      pattern: "useUserEarnings"
---

<objective>
Add "All Pools" and "My Pools" tabs to the pools page, with "My Pools" showing only pools where the user has GM tokens and displaying PnL. Add utilization percentage to pool stats.

Purpose: Users need to see their active positions and performance at a glance. "My Pools" with PnL was explicitly requested by the user.
Output: Enhanced pools page with tab filtering, user-specific pool views, and richer statistics.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-liquidity-swaps/05-CONTEXT.md

# Key existing files
@src/pages/Pools/Pools.tsx
@src/components/GmList/GmList.tsx
@src/components/GmList/GmListItem.tsx
@src/components/GmList/GmTokensTotalBalanceInfo.tsx
@src/components/GmList/useFilterSortPools.tsx
@src/domain/synthetics/markets/useUserEarnings.ts
@src/domain/synthetics/markets/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add All Pools / My Pools tabs and utilization to pools page</name>
  <files>
    src/pages/Pools/Pools.tsx
    src/components/GmList/GmList.tsx
    src/components/GmList/useFilterSortPools.tsx
    src/components/GmList/GmListItem.tsx
  </files>
  <action>
    **Part A: Add pool tab state to Pools.tsx**

    In `src/pages/Pools/Pools.tsx`:
    1. Add a `poolsTab` state: `const [poolsTab, setPoolsTab] = useState<"all" | "my">("all");`
    2. Add tab UI above the GmList component using the existing `Tabs` component from `components/Tabs/Tabs` (same one used in GmSwapBoxHeader):
       - Options: `[{ value: "all", label: "All Pools" }, { value: "my", label: "My Pools" }]`
       - Use `Trans` from `@lingui/macro` for labels
       - Style the tabs to fit the page layout (match existing design patterns)
    3. Pass `poolsTab` to the `GmList` component as a new `activeTab` prop

    **Part B: Filter pools by user balance in GmList.tsx**

    In `src/components/GmList/GmList.tsx`:
    1. Add `activeTab: "all" | "my"` to the `Props` type
    2. When `activeTab === "my"`:
       - Filter `filteredGmTokens` to only include tokens where `token.balance && token.balance > 0n`
       - If user is not connected (`!active`), show a message: "Connect wallet to see your pools"
       - If connected but no pools have balance, show: "You have no pool positions"
    3. Pass `activeTab` to `GmListItem` so it can conditionally show PnL

    **Part C: Add PnL display to My Pools in GmListItem.tsx**

    In `src/components/GmList/GmListItem.tsx`:
    1. Accept a new `showPnl` boolean prop (true when `activeTab === "my"`)
    2. When `showPnl` is true, show a PnL value in the wallet column or as an additional info:
       - Use `marketEarnings?.total` from the existing `useUserEarnings` hook (already called in GmListItem)
       - Format as USD: green for positive, red for negative
       - Display as "PnL: +$X.XX" or "PnL: -$X.XX" below the wallet balance
    3. On mobile, add PnL as a `SyntheticsInfoRow` when `showPnl` is true

    **Part D: Add utilization percentage to pool rows**

    In `src/components/GmList/GmListItem.tsx`:
    1. Compute utilization for each pool:
       - Utilization = (longOpenInterest + shortOpenInterest) / (longPoolAmount + shortPoolAmount) as a percentage
       - The `marketOrGlv` object (from `selectGlvAndMarketsInfoData`) contains `longInterestUsd`, `shortInterestUsd`, and pool amounts as `longPoolAmountUsd`, `shortPoolAmountUsd` (or similar fields). Check the MarketInfo type in `sdk/src/types/` to find the exact field names for open interest and pool amounts in USD.
       - Guard against division by zero (if pool amounts are 0, show "0%" or "N/A")
    2. Desktop: Add a new column "UTIL" between the existing APY and Performance columns
       - Update the `GmList.tsx` table header to add the UTIL column header
       - Display utilization as a percentage with 1 decimal: "42.3%"
    3. Mobile: Add utilization as a `SyntheticsInfoRow` with label "Utilization"
    4. Format using `formatPercentage` or manual `(util * 100).toFixed(1) + "%"` depending on the bigint math needed

    **Important note on file ownership:** This plan modifies `GmListItem.tsx` which is also modified in Plan 01. However, Plan 01 adds Buy/Sell buttons to the action column, while this plan adds utilization column and PnL display. Since both plans are Wave 1, the executor should be aware:
    - If executing sequentially: second plan reads the file modified by the first
    - If executing in parallel: merge conflicts are possible on GmListItem.tsx

    Recommend executing Plan 01 first, then Plan 02, since Plan 02 touches the same file.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors. Verify the Pools page renders tabs, the "My Pools" tab filters correctly, and utilization appears in pool rows.
  </verify>
  <done>
    Pools page shows "All Pools" and "My Pools" tabs. "My Pools" filters to pools where user has GM balance and shows PnL. All pool rows display utilization percentage.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify pools page tabs, My Pools PnL, and utilization display</name>
  <what-built>
    Enhanced pools page with All Pools / My Pools tabs, user PnL in My Pools tab, and utilization percentage on all pool rows. Combined with Plan 01's Sell GM functionality.
  </what-built>
  <how-to-verify>
    1. Navigate to the Pools page at http://localhost:3113/#/pools
    2. Verify "All Pools" and "My Pools" tabs are visible above the pool list
    3. Click "All Pools" tab - all 6 pools should be listed
    4. Check that each pool row shows a "UTIL" or utilization percentage column
    5. Connect your wallet
    6. Click "My Pools" tab - should show only pools where you have GM token balance
    7. If you have GM tokens: verify PnL is displayed (green for profit, red for loss)
    8. If no GM tokens: verify "You have no pool positions" message appears
    9. Disconnect wallet and click "My Pools" - should show "Connect wallet to see your pools"
    10. Verify the "Buy GM" and "Sell GM" buttons appear on each pool row (from Plan 01)
    11. Click "Sell GM" on a pool where you have GM tokens - should navigate to pool details with Sell GM form
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the pools page layout, tabs, PnL display, or utilization</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Pools page has "All Pools" and "My Pools" tabs
3. "My Pools" filters by user GM balance
4. PnL displays for user's pools in "My Pools" tab
5. Utilization percentage shows on all pool rows
6. Empty states work correctly (no wallet, no positions)
</verification>

<success_criteria>
- "All Pools" tab shows all 6 market pools with TVL, APY, utilization, performance
- "My Pools" tab shows only pools with user GM balance, including PnL
- Utilization percentage is calculated and displayed on each pool row
- Empty states are handled (no wallet connected, no pool positions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-liquidity-swaps/05-02-SUMMARY.md`
</output>
