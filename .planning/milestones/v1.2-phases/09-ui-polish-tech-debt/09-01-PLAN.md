---
phase: 09-ui-polish-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/SyntheticsPage/SyntheticsPage.tsx
  - src/pages/Pools/Pools.tsx
  - src/components/Header/Header.tsx
  - src/components/PositionList/PositionList.tsx
  - src/components/OrderList/OrderList.tsx
  - src/components/TradeBox/TradeBox.tsx
  - src/components/PositionSeller/PositionSeller.tsx
  - src/components/PositionEditor/PositionEditor.tsx
  - src/lib/wallets/WalletProvider.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "All 6 market pages render with consistent spacing, fonts, and component sizes"
    - "Loading states show skeletons or spinners — never blank content areas"
    - "Empty positions/orders tables show column headers with no data rows — no confusing blank space"
    - "Transaction success toasts include a BaseScan explorer link"
    - "Wallet disconnect does not leave stale data or cause console errors"
    - "Trade page and pools page look clean and professional at desktop widths"
  artifacts:
    - path: "src/pages/SyntheticsPage/SyntheticsPage.tsx"
      provides: "Polished trade page layout"
    - path: "src/pages/Pools/Pools.tsx"
      provides: "Polished pools page layout"
    - path: "src/components/PositionList/PositionList.tsx"
      provides: "Empty state for positions table"
    - path: "src/components/OrderList/OrderList.tsx"
      provides: "Empty state for orders table"
  key_links:
    - from: "Transaction success handler"
      to: "Toast with BaseScan link"
      via: "helperToast or notification util"
      pattern: "basescan\\.org|View on"
    - from: "Wallet disconnect"
      to: "State cleanup"
      via: "wagmi disconnect handler"
      pattern: "disconnect|onDisconnect"
---

<objective>
UI audit and polish across all pages — fix visual inconsistencies, add loading/empty states, polish wallet UX, and make the app demo-ready for investors.

Purpose: A first-time visitor should see a professional, consistent interface with no rough edges, broken layouts, or confusing blank states.
Output: Polished trade page, pools page, loading states, empty states, wallet UX, and transaction feedback.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-polish-tech-debt/09-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Visual consistency audit and fix across trade page and pools page</name>
  <files>
    src/pages/SyntheticsPage/SyntheticsPage.tsx
    src/pages/Pools/Pools.tsx
    src/components/Header/Header.tsx
    src/components/TradeBox/TradeBox.tsx
    src/components/PositionList/PositionList.tsx
    src/components/OrderList/OrderList.tsx
    src/components/PositionSeller/PositionSeller.tsx
    src/components/PositionEditor/PositionEditor.tsx
  </files>
  <action>
    Audit and fix visual consistency across the two primary pages (Trade and Pools). Reference Hyperliquid and GMX v2 as the quality bar — clean, data-dense, professional.

    1. **Trade page (SyntheticsPage):** Review layout spacing, alignment of the chart/tradebox/positions panels. Fix any inconsistent padding, misaligned elements, or orphaned whitespace. Ensure all 6 markets render identically (switch between ETH, BTC, EUR, GBP, GOLD, JPY and verify same layout).

    2. **Pools page:** Review table alignment, header consistency, column widths. Ensure My Pools and All Pools tabs have consistent styling.

    3. **Empty states for tables:** PositionList and OrderList should render column headers with an empty body when no data exists — no extra messaging or CTAs (per user decision). Just clean empty tables.

    4. **Loading states:** Add skeleton loaders or spinners (Claude's discretion per component) for:
       - Position list while positions load
       - Order list while orders load
       - Pool stats while data loads
       - TradeBox while market data loads
    Use existing loading patterns in the codebase where they exist. For new ones, use a simple skeleton shimmer or spinner consistent with the app's existing style.

    5. **Error states:** Ensure API/data errors show toast notifications (non-blocking, auto-dismiss) via the existing toast system. Errors should not leave blank or broken UI — fall back to empty state gracefully.

    Do NOT add mobile responsive redesign here — just fix desktop layout issues. Do NOT add new features.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vite build 2>&1 | tail -5</automated>
    <manual>Visit each of the 6 market pages and verify consistent layout. Check Pools page. Verify empty states render cleanly when no positions/orders exist.</manual>
  </verify>
  <done>All 6 market pages render with identical layout structure. Empty positions/orders tables show headers with no rows. Loading states are visible during data fetch. No visual rough edges on trade or pools pages at desktop widths.</done>
</task>

<task type="auto">
  <name>Task 2: Wallet UX polish — disconnect fix, connect prompts, transaction toasts</name>
  <files>
    src/lib/wallets/WalletProvider.tsx
    src/components/TradeBox/TradeBox.tsx
    src/components/PositionSeller/PositionSeller.tsx
    src/components/PositionEditor/PositionEditor.tsx
    src/domain/synthetics/orders/simulateExecuteTxn.tsx
  </files>
  <action>
    Polish wallet connection UX for demo-readiness:

    1. **Wallet disconnect bug:** Investigate what happens when a user disconnects their wallet mid-session. Check if stale position/order data persists, if console errors fire, if context state is cleaned up. Fix any issues — on disconnect, state should reset cleanly to the "not connected" view without errors.

    2. **Pre-connection action buttons:** Verify that action buttons (Deposit, Trade, Close Position, etc.) show "Connect Wallet" text and trigger the wallet connect flow when clicked and no wallet is connected. Fix any buttons that show disabled/broken state instead.

    3. **Transaction success toasts:** Update transaction success notifications to include a "View on BaseScan" link. Format: `https://sepolia.basescan.org/tx/{txHash}`. Use the existing toast/notification system. This applies to deposits, withdrawals, order creation, position close, and position edits.

    4. **Pending transaction feedback:** Verify that pending transactions show a toast with spinner. The action button should return to its normal state (not stay disabled), while the toast shows "Transaction pending..." until confirmation.

    Do NOT change the wrong-network handling (keep current behavior per user decision). Do NOT change wallet connect modal styling.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vite build 2>&1 | tail -5</automated>
    <manual>Test: connect wallet, submit a deposit, verify success toast has BaseScan link. Disconnect wallet and verify no console errors, clean state reset. Visit trade page without wallet connected and verify buttons show "Connect Wallet".</manual>
  </verify>
  <done>Wallet disconnect resets state cleanly without errors. Action buttons show "Connect Wallet" when disconnected. Transaction success toasts include BaseScan explorer link. Pending transactions show toast with spinner while button remains usable.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no TypeScript errors introduced)
- `npx vite build` succeeds
- Visual inspection: all 6 markets consistent, empty states clean, loading states present
- Wallet flow: connect -> transact -> see BaseScan link -> disconnect -> clean state
</verification>

<success_criteria>
- A first-time user navigating from pools to trading to position management encounters no confusing states, broken layouts, or console errors
- Loading states, empty states, and error messages look intentional (not placeholder)
- All 6 market pages render with consistent visual styling (same fonts, spacing, component sizes)
- Transaction feedback includes explorer links for verification
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-polish-tech-debt/09-01-SUMMARY.md`
</output>
