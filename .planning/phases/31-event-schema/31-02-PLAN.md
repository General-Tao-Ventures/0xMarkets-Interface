---
phase: 31-event-schema
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - /Users/ken/Projects/0xM/data-verification-service/src/migrate.ts
  - /Users/ken/Projects/0xM/data-verification-service/package.json
  - /Users/ken/Projects/0xM/data-verification-service/Dockerfile
autonomous: true
requirements: [SCHEMA-04]

must_haves:
  truths:
    - "Migration runner reads all SQL files from prisma/sql/ and executes them via pg client"
    - "Migration runner is idempotent -- safe to run on every startup"
    - "Dockerfile CMD runs migration runner before the app starts"
    - "pg dependency is installed in package.json"
  artifacts:
    - path: "src/migrate.ts"
      provides: "Node.js migration runner that executes SQL files"
      exports: ["runMigrations"]
      min_lines: 20
    - path: "package.json"
      provides: "pg dependency added"
      contains: "\"pg\""
    - path: "Dockerfile"
      provides: "Updated CMD to run migrations"
      contains: "migrate"
  key_links:
    - from: "src/migrate.ts"
      to: "prisma/sql/*.sql"
      via: "fs.readdir + fs.readFile"
      pattern: "readdir.*sql.*readFile"
    - from: "Dockerfile"
      to: "src/migrate.ts"
      via: "CMD runs compiled migrate.js before index.js"
      pattern: "node dist/migrate"
    - from: "src/migrate.ts"
      to: "PostgreSQL"
      via: "pg Client connection using DATABASE_URL"
      pattern: "new Client|Pool"
---

<objective>
Create the Node.js migration runner and wire it into the Docker startup sequence.

Purpose: The migration runner bridges the SQL files (Plan 01) and the running application. It must execute all SQL files in order on every container startup, using CREATE IF NOT EXISTS semantics to be idempotent. This is the mechanism that gets the schema into the database.

Output: A migrate.ts module, pg dependency in package.json, and updated Dockerfile CMD.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-event-schema/31-CONTEXT.md

Existing service structure:
@/Users/ken/Projects/0xM/data-verification-service/src/index.ts
@/Users/ken/Projects/0xM/data-verification-service/src/config.ts
@/Users/ken/Projects/0xM/data-verification-service/package.json
@/Users/ken/Projects/0xM/data-verification-service/Dockerfile
@/Users/ken/Projects/0xM/data-verification-service/tsconfig.json

Prior plan output (SQL files):
@.planning/phases/31-event-schema/31-01-SUMMARY.md
</context>

<interfaces>
<!-- Existing patterns from the data-verification-service -->

Logging pattern (from index.ts):
```typescript
import { logger } from "./utils/logger.js";
const log = logger.child({ module: "migrate" });
```

Config pattern (from config.ts):
```typescript
import { config } from "./config.js";
// config.databaseUrl is the DATABASE_URL
```

ESM module: package.json has "type": "module", so use import/export syntax.
TypeScript compiled to dist/ directory.

Current Dockerfile CMD:
```dockerfile
CMD ["sh", "-c", "pnpm db:migrate:deploy && node dist/index.js"]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add pg dependency and create migration runner</name>
  <files>
    /Users/ken/Projects/0xM/data-verification-service/package.json
    /Users/ken/Projects/0xM/data-verification-service/src/migrate.ts
  </files>
  <action>
1. Add `pg` to dependencies and `@types/pg` to devDependencies in package.json. Run `cd /Users/ken/Projects/0xM/data-verification-service && pnpm add pg && pnpm add -D @types/pg` to install.

2. Create src/migrate.ts with a `runMigrations` function:
   - Import pg Client, fs/promises (readdir, readFile), path, and the logger
   - Create a pg Client using config.databaseUrl (NOT Prisma -- raw pg for schema-namespaced tables)
   - Read all .sql files from prisma/sql/ directory, sorted alphabetically (001, 002, ... 010)
   - For determining the SQL directory path: use `new URL('../prisma/sql', import.meta.url)` to resolve relative to the compiled file location (since the prisma/ dir is copied alongside dist/ in Docker). Actually, since the Docker layout is /app/prisma/sql and /app/dist/migrate.js, use path.join(process.cwd(), 'prisma', 'sql') which works in both dev and Docker.
   - Execute each file's SQL content sequentially via client.query()
   - Log each file name and success/failure via pino logger
   - On error: log the error with file name and SQL statement that failed, then throw (do NOT swallow errors -- startup should abort if schema creation fails)
   - Disconnect pg Client in finally block

3. Also export a standalone runner: if the file is run directly (`node dist/migrate.js`), call runMigrations() and exit. Check via `import.meta.url === pathToFileURL(process.argv[1]).href`.

The function signature: `export async function runMigrations(): Promise<void>`
  </action>
  <verify>
    <automated>cd /Users/ken/Projects/0xM/data-verification-service && grep '"pg"' package.json && ls src/migrate.ts && npx tsc --noEmit src/migrate.ts 2>&1 | head -20</automated>
  </verify>
  <done>pg dependency installed, migrate.ts compiles without errors, exports runMigrations function that reads and executes all SQL files from prisma/sql/ in order.</done>
</task>

<task type="auto">
  <name>Task 2: Update Dockerfile CMD and add npm script</name>
  <files>
    /Users/ken/Projects/0xM/data-verification-service/Dockerfile
    /Users/ken/Projects/0xM/data-verification-service/package.json
  </files>
  <action>
1. Add a "db:migrate:sql" script to package.json scripts: `"db:migrate:sql": "node dist/migrate.js"`

2. Update the Dockerfile CMD to run the SQL migration runner AFTER Prisma migrations but BEFORE the app:
   - Change from: `CMD ["sh", "-c", "pnpm db:migrate:deploy && node dist/index.js"]`
   - Change to: `CMD ["sh", "-c", "pnpm db:migrate:deploy && node dist/migrate.js && node dist/index.js"]`
   - This ensures: (1) Prisma tables exist first, (2) event schema tables created, (3) app starts

3. Verify the build stage in Dockerfile compiles all TypeScript (it does -- `pnpm build` runs tsc), so migrate.ts will be compiled to dist/migrate.js automatically.
  </action>
  <verify>
    <automated>cd /Users/ken/Projects/0xM/data-verification-service && grep "migrate.js" Dockerfile && grep "db:migrate:sql" package.json && pnpm build 2>&1 | tail -5 && ls dist/migrate.js</automated>
  </verify>
  <done>Dockerfile CMD runs Prisma migrations, then SQL migrations, then app. npm script exists for manual migration runs. Project builds successfully with migrate.js in dist/.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds -- migrate.ts compiles alongside all other TypeScript
2. dist/migrate.js exists in build output
3. Dockerfile CMD contains three chained commands: pnpm db:migrate:deploy, node dist/migrate.js, node dist/index.js
4. package.json has pg in dependencies and @types/pg in devDependencies
5. migrate.ts uses pino logger (consistent with rest of codebase)
6. migrate.ts handles errors properly (logs and throws, doesn't swallow)
</verification>

<success_criteria>
- Migration runner compiles and is included in build output
- pg dependency installed
- Dockerfile CMD chains Prisma migrations -> SQL migrations -> app start
- Migration runner reads SQL files in alphabetical order and executes via pg client
- Migration runner is idempotent (safe on every startup, relies on IF NOT EXISTS in SQL)
</success_criteria>

<output>
After completion, create `.planning/phases/31-event-schema/31-02-SUMMARY.md`
</output>
