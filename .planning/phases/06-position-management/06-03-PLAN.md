---
phase: 06-position-management
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/TradeBox/TradeBox.tsx
  - src/components/TradeBox/TradeBoxRows/LimitAndTPSLRows.tsx
  - src/components/TradeBox/hooks/useTradeButtonState.tsx
  - src/components/TradeBox/hooks/useTradeboxTransactions.tsx
  - src/components/TradeBox/components/SideOrderEntries.tsx
  - src/components/OrderList/OrderList.tsx
  - src/components/OrderItem/OrderItem.tsx
  - src/domain/synthetics/orders/cancelOrdersTxn.ts
  - src/domain/synthetics/sidecarOrders/useSidecarOrders.ts
autonomous: true
requirements:
  - POS-03
  - POS-04

must_haves:
  truths:
    - "User can switch to Limit order type in the TradeBox and set a trigger price"
    - "Limit order appears in the Orders tab as pending until the price is reached"
    - "User can cancel a pending limit order from the Orders tab"
    - "User can set SL/TP on a new position during order placement via the TradeBox"
    - "User can set SL/TP on an existing position from the positions table"
    - "SL/TP orders appear in the Orders tab and execute when trigger price is hit"
  artifacts:
    - path: "src/components/TradeBox/TradeBox.tsx"
      provides: "Trade mode selector (Market / Limit) and limit price input"
    - path: "src/components/TradeBox/TradeBoxRows/LimitAndTPSLRows.tsx"
      provides: "SL/TP entry fields in the TradeBox"
    - path: "src/components/OrderList/OrderList.tsx"
      provides: "Pending orders list with cancel button"
    - path: "src/components/OrderItem/OrderItem.tsx"
      provides: "Individual order row with type, trigger price, size, and cancel action"
    - path: "src/domain/synthetics/sidecarOrders/useSidecarOrders.ts"
      provides: "Sidecar order (SL/TP) state management"
  key_links:
    - from: "src/components/TradeBox/TradeBox.tsx"
      to: "src/domain/synthetics/trade/useTradeboxState.ts"
      via: "tradeMode selector (Market/Limit)"
      pattern: "TradeMode\\.Limit|onSelectTradeMode"
    - from: "src/components/TradeBox/TradeBoxRows/LimitAndTPSLRows.tsx"
      to: "src/domain/synthetics/sidecarOrders/useSidecarOrders.ts"
      via: "SL/TP entry state"
      pattern: "stopLoss|takeProfit|useSidecarOrders"
    - from: "src/components/OrderList/OrderList.tsx"
      to: "src/domain/synthetics/orders/cancelOrdersTxn.ts"
      via: "cancel order action"
      pattern: "cancelOrdersTxn|onCancelOrder"
---

<objective>
Enable limit orders and stop-loss/take-profit (SL/TP) orders. User can place a limit order to open at a specific price, and can attach SL/TP orders to new or existing positions. Pending orders appear in the Orders tab with cancel support.

Purpose: Limit orders and SL/TP are essential trading tools. Limit orders let users enter at target prices, while SL/TP provides automated risk management for open positions.

Output: Working limit order placement, SL/TP attachment (both at order time and on existing positions), pending order display, and order cancellation.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-position-management/06-CONTEXT.md
@.planning/phases/06-position-management/06-01-SUMMARY.md

Key context:
- TradeBox already has a trade mode selector (Market/Limit/Trigger) in `TradeBoxHeaderTabs` and `tradeboxConstants.tsx`.
- The `tradeMode` field in `useTradeboxState` controls which UI appears (limit price input, trigger price, etc.).
- `LimitAndTPSLRows.tsx` already renders SL/TP entry fields using `useSidecarOrders` and `SideOrderEntries`.
- Sidecar orders (SL/TP) are submitted alongside the main order in `sendBatchOrderTxn` via `useSidecarOrderPayloads`.
- The OrderList and OrderItem components already exist from the GMX fork.
- `cancelOrdersTxn.ts` handles order cancellation via ExchangeRouter.
- Limit orders create `LimitIncrease` (OrderType 3) or `LimitDecrease` (OrderType 5) orders.
- SL/TP create `StopLossDecrease` (OrderType 6) and `LimitDecrease` (OrderType 5) orders.
- User decisions: Order type selection via dropdown (Market/Limit), percentage shortcuts for limit price (-1%, -5%, +1%, +5%), SL/TP settable during order placement AND inline on position row, configurable SL/TP close size.
- Claude's discretion: Pending orders display layout (using existing Orders tab from GMX fork).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable limit order placement and pending order display</name>
  <files>
    src/components/TradeBox/TradeBox.tsx
    src/components/TradeBox/hooks/useTradeButtonState.tsx
    src/components/TradeBox/hooks/useTradeboxTransactions.tsx
    src/components/OrderList/OrderList.tsx
    src/components/OrderItem/OrderItem.tsx
    src/domain/synthetics/orders/cancelOrdersTxn.ts
  </files>
  <action>
    1. **Trade mode switching**: The TradeBox already has `TradeMode.Market`, `TradeMode.Limit`, and `TradeMode.Trigger` modes. In `TradeBox.tsx`, verify:
       - The trade mode selector (tabs or dropdown) renders correctly and switches between Market and Limit
       - When Limit mode is selected, the trigger price input field appears (`triggerPriceInputValue` / `setTriggerPriceInputValue` from useTradeboxState)
       - User decision: Use dropdown selector for Market/Limit within the trade box. Check if the existing mode selector in `TradeBoxHeaderTabs.tsx` or the inline Tabs component matches this UX. If it uses tabs, that's acceptable (Claude's discretion on exact layout). The key is that both Market and Limit modes are accessible.

    2. **Limit price input with percentage shortcuts**: Per user decision, add percentage shortcut buttons for the limit price: -1%, -5%, +1%, +5% from current mark price. Check if `TradeBox.tsx` already renders a trigger price input when `isLimit` is true. If shortcut buttons don't exist:
       - Below the trigger price input, add a row of buttons: "-5%", "-1%", "+1%", "+5%"
       - Each button calculates the target price from `selectTradeboxMarkPrice` and sets `setTriggerPriceInputValue`
       - Format: `markPrice * (1 + percentage)` converted to the correct decimal format via `formatAmountFree`
       - Style: Small pill buttons in a row, consistent with the existing TradeBox styling (Tailwind, slate theme)

    3. **Limit order validation**: In `useTradeButtonState.tsx`, verify the button state logic handles limit orders:
       - Trigger price must be set (non-zero)
       - For Long Limit: trigger price should be below current mark price
       - For Short Limit: trigger price should be above current mark price
       - The `getIncreaseError` or trade-type-specific validation should handle this. If it doesn't differentiate Market vs Limit validation, check for missing trigger price validation.

    4. **Limit order submission**: In `useTradeboxTransactions.tsx`, verify that when `tradeMode === TradeMode.Limit`, the order params use `OrderType.LimitIncrease` (3) instead of `OrderType.MarketIncrease` (2). The `selectTradeBoxCreateOrderParams` selector should handle this based on trade mode. Verify the trigger price and acceptable price are included in the order params.

    5. **Pending orders display**: The OrderList and OrderItem components already exist. Verify:
       - After submitting a limit order, it appears in the Orders tab
       - OrderItem shows: Market, Type (Limit), Side (Long/Short), Size, Trigger Price, Mark Price
       - Cancel button per order is present and functional
       - `cancelOrdersTxn.ts` calls `ExchangeRouter.cancelOrder(key)` — verify this works

    6. **Order cancellation**: Test cancelling a pending limit order:
       - Click cancel on the order row
       - Transaction submits to cancel the order
       - Order disappears from the Orders tab
       - Collateral is returned to the user's wallet
  </action>
  <verify>
    1. Switch to Limit mode in TradeBox — trigger price input appears
    2. Click percentage shortcut buttons — price fills with correct calculated value
    3. Submit a limit order with price below current market (for Long) — transaction succeeds
    4. Limit order appears in the Orders tab with correct details
    5. Cancel the limit order — order disappears, collateral returned
    6. Run `yarn typecheck` — no TypeScript errors
  </verify>
  <done>
    Limit orders can be placed with trigger price (including percentage shortcuts). Pending limit orders appear in the Orders tab. Orders can be cancelled from the UI.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enable SL/TP order attachment on new and existing positions</name>
  <files>
    src/components/TradeBox/TradeBoxRows/LimitAndTPSLRows.tsx
    src/components/TradeBox/components/SideOrderEntries.tsx
    src/components/TradeBox/hooks/useTradeboxTransactions.tsx
    src/domain/synthetics/sidecarOrders/useSidecarOrders.ts
    src/domain/synthetics/sidecarOrders/useSidecarEntries.ts
    src/components/PositionItem/PositionItem.tsx
    src/components/OrderEditor/OrderEditor.tsx
  </files>
  <action>
    1. **SL/TP during order placement**: The `LimitAndTPSLRows.tsx` component already renders SL/TP entry fields using `useSidecarOrders`. Verify:
       - When placing a market or limit increase order, SL/TP fields are visible in the TradeBox
       - `SideOrderEntries.tsx` renders input fields for stop loss price and take profit price
       - Each entry supports both price level input and percentage from entry price (-5%, -10% for SL; +5%, +10% for TP) per user decision
       - If percentage shortcut buttons don't exist for SL/TP entries, add them following the same pattern as the limit price shortcuts
       - SL/TP close size is configurable per user decision — each SL/TP entry should have a "Close %" or "Size" input. Check if `SidecarOrderEntry` type has a `percentage` or `sizePercent` field. If the existing code only supports full-position SL/TP, add a size percentage field (default 100%).

    2. **SL/TP batch submission**: When submitting an increase order with SL/TP, `useSidecarOrderPayloads.ts` builds the sidecar order payloads that are included in the batch. Verify:
       - `sendBatchOrderTxn` includes both the increase order AND the SL/TP decrease orders in a single multicall
       - SL creates `StopLossDecrease` (OrderType 6) with trigger price
       - TP creates `LimitDecrease` (OrderType 5) with trigger price
       - The `sizeDeltaUsd` on each SL/TP order matches the user's configured close size (not always full position)

    3. **SL/TP on existing positions**: Per user decision, SL/TP can be set inline on the position row. Check `PositionItem.tsx` for:
       - An SL/TP status column or clickable area in the position row
       - If clicked, it should open an order editor or inline input for setting SL/TP
       - The existing `OrderEditor.tsx` may handle this — check if it supports creating new SL/TP orders for a position
       - The `onOrdersClick` callback on PositionItem routes to the Orders tab where existing SL/TP orders can be edited

       The existing GMX code likely has this wiring through `PositionDropdown` (context menu on position row) or through the SL/TP column click. Trace the flow:
       - `PositionItem` -> click SL/TP field -> should open order creation for that position
       - If this flow doesn't exist cleanly, wire it: add an "Add SL/TP" button/link in the PositionItem that opens a modal or popover with stop loss and take profit price inputs, then submits via `sendBatchOrderTxn` with StopLossDecrease/LimitDecrease orders

    4. **SL/TP display in position row**: The user decided on "SL/TP status" as a column in the positions table. Verify PositionItem shows:
       - If SL/TP orders exist for this position: show the trigger prices (e.g., "SL: $3,200 / TP: $3,800")
       - If no SL/TP: show "—" or "Add" link
       - The existing code may use `position.orders` or query orders filtered by position key

    5. **SL/TP orders in Orders tab**: After setting SL/TP, verify they appear in the Orders tab:
       - Type shows "Stop Loss" or "Take Profit"
       - Trigger price is correct
       - Size matches the configured close amount
       - Can be cancelled individually
  </action>
  <verify>
    1. Open a market order with SL and TP set — all 3 orders submitted in one transaction
    2. SL and TP orders appear in the Orders tab with correct trigger prices
    3. Position row shows SL/TP status (trigger prices or "Add" link)
    4. Click SL/TP on an existing position (that has no SL/TP) — can add new SL/TP orders
    5. Cancel a SL/TP order — it's removed from Orders tab, position SL/TP status updates
    6. Percentage shortcut buttons work for SL/TP price input
    7. Run `yarn typecheck` — no TypeScript errors
  </verify>
  <done>
    SL/TP can be set during order placement (as sidecar orders in the same batch tx). SL/TP can be added to existing positions from the position row. SL/TP orders appear in the Orders tab and can be cancelled. Close size is configurable.
  </done>
</task>

</tasks>

<verification>
- Limit order submission works with trigger price and percentage shortcuts
- Pending limit orders appear in Orders tab with cancel support
- SL/TP settable during order placement (sidecar orders)
- SL/TP settable on existing positions from position row
- SL/TP orders visible in Orders tab with correct details
- Order cancellation works for both limit and SL/TP orders
- No TypeScript errors (`yarn typecheck` passes)
</verification>

<success_criteria>
User can place limit orders at specified prices with percentage shortcuts. User can attach SL/TP to new positions during order placement and to existing positions inline. All pending orders (limit, SL, TP) appear in the Orders tab and can be cancelled. SL/TP close size is configurable.
</success_criteria>

<output>
After completion, create `.planning/phases/06-position-management/06-03-SUMMARY.md`
</output>
