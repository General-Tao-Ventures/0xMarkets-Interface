---
phase: 25-liquidation-pipeline-verification
task: 2
total_tasks: 2
status: in-progress
last_updated: 2026-02-28T08:40:00.000Z
---

<current_state>
Phase 25 Task 2 is IN PROGRESS. Position created and collateral withdrawn. Scanner bug identified.

Key achievement: A real position exists on-chain that SHOULD be liquidatable (~72x leverage).
The liquidation scanner sees it but reports "not liquidatable" due to a price precision bug.
</current_state>

<completed_work>

- Transferred 0.01 ETH from deployer wallet (0x9724...1200, key in 0xmarkets_contract/.env ACCOUNT_KEY) to keeper wallet
- Fixed test-liquidation.ts: initialCollateralDeltaAmount was 0n, now set to COLLATERAL_AMOUNT
- Fixed test-liquidation.ts: collateral raised from $5 to $10, sizes calibrated to pool headroom
- Discovered positions WERE being created all along — the DataStore key derivation in the script was wrong:
  - Script used: keccak256(abi.encode(positionKey, "sizeInUsd")) — WRONG
  - Correct: keccak256(abi.encode(positionKey, keccak256(abi.encode("SIZE_IN_USD"))))
- Pool state: $10,256 USDC, 0.95 reserve factor, OI Long ~$4,600, OI Short ~$4,580
- Created withdraw-collateral.ts to drain collateral from existing LONG position
- Successfully withdrew 260 USDC collateral (TX 0xa36f1475...)
- Position now at: $4,570 size, $63.21 USDC collateral, ~72x leverage
- Position key: 0xa57c02dfbb8f5bbebfe71e66380bbb618452ef2028691eced7b115d8e56707c2
</completed_work>

<bug_found>
SCANNER PRICE PRECISION BUG:
- Scanner calls Reader.isPositionLiquidatable() with market prices
- Returns remainingCollateralUsd = ~4.6 BILLION for a $63 collateral position
- Root cause: ALL Lazer simulations fail ("lazer simulation failed" for every token)
- Scanner falls back to Hermes HTTP prices
- Hermes prices are likely in WRONG precision for the Reader contract
- The Reader contract expects prices in GMX format (with specific decimal precision)
- Hermes prices may not be scaled correctly, causing the liquidation check to miscalculate

Where to look:
- keeper-service/src/core/scanner.ts: getMarketPrices() method — how Hermes prices are formatted
- Compare Hermes price format vs what Reader.isPositionLiquidatable expects
- Check price precision: GMX uses prices with 30 decimal precision in some places
- The Lazer simulation failures may be the real issue — fixing Lazer would fix prices

Scanner log pattern:
- "lazer simulation failed" for 0x4200...0006 (WETH), 0xd8a6... (WBTC), 0xfddfe4... (USDC), etc.
- Error: getOraclePrice reverted with 0x2b6e7c3f
- Hermes fallback then provides 7 prices, 0 from Lazer
</bug_found>

<remaining_work>

1. FIX the scanner price precision bug:
   - Investigate getMarketPrices() in scanner.ts
   - Compare Hermes price format vs Reader contract expectations
   - Fix the price scaling so Reader.isPositionLiquidatable returns correct values
   - OR fix the Lazer simulation failures so scanner uses on-chain prices

2. After fix, the scanner should detect our position as liquidatable:
   - Position key: 0xa57c02dfbb8f5bbebfe71e66380bbb618452ef2028691eced7b115d8e56707c2
   - The executor should submit executeLiquidation TX
   - The confirmator should record it in PostgreSQL

3. Verify end-to-end:
   - Check liquidation_candidates table for EXECUTED status
   - Check liquidation_executions table for MINED TX
   - Verify on Basescan

4. Update docs: 25-04-SUMMARY.md, STATE.md, ROADMAP.md
</remaining_work>

<decisions_made>

- Deployer wallet key found in 0xmarkets_contract/.env as ACCOUNT_KEY (not PRIVATE_KEY)
- initialCollateralDeltaAmount MUST be set to match collateral sent to OrderVault
- GMX v2 DataStore position keys use hashed string constants (e.g., keccak256("SIZE_IN_USD")), not raw strings
- Collateral withdrawal approach: MarketDecrease with sizeDeltaUsd=0, orderType=4
- Min collateral for current position: ~$43.26 (from UnableToWithdrawCollateral error)
- Pool is saturated on both sides — can't add more size, only work with existing position
</decisions_made>

<context>
The e2e test infrastructure works. Orders are created and executed properly.
The ONLY remaining issue is the scanner's price precision bug preventing liquidation detection.
Once the scanner correctly identifies the 72x leveraged position as liquidatable,
the executor/confirmator pipeline should complete the end-to-end verification.

Files created this session:
- e2e/withdraw-collateral.ts (collateral drain utility)
- e2e/test-liquidation.ts (updated with proper collateral amounts)
</context>

<next_action>
1. Read keeper-service/src/core/scanner.ts getMarketPrices() method
2. Compare price precision between Hermes HTTP and Reader contract expectations
3. Fix the precision mismatch
4. Restart keeper-service and watch for liquidation detection
</next_action>
