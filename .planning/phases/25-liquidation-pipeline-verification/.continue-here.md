---
phase: 25-liquidation-pipeline-verification
task: 2
total_tasks: 2
plan: 25-02
status: in_progress
last_updated: 2026-02-28T00:28:06.460Z
---

<current_state>
Phase 25 execute-phase workflow: Wave 1 (Plan 25-01) complete. Wave 2 (Plan 25-02) in progress — Task 1 committed (test-liquidation.ts), Task 2 checkpoint (full pipeline E2E verification) is active.

CRITICAL DISCOVERY: The PythLazerFeedProvider address was corrected MID-SESSION. Plan 25-01 incorrectly identified `0x8a3eb351...` as the correct address based on `getStoredPrice` returning data. On-chain investigation revealed the ACTUAL enabled oracle provider is `0xc5810FC1932e44866bD0D041FbfB08d8AC2A67d6`.

All .env files and config.ts defaults have been corrected to `0xc5810FC...` but the keeper-service commit (e319e36 in keeper-service repo) still has the WRONG default in config.ts. This needs an amend or follow-up commit.
</current_state>

<completed_work>

## Plan 25-01 (Wave 1) — Keeper Configuration Bug Fixes
- Task 1: Fix PythLazerFeedProvider address and oracle mode — Committed (e319e36 in keeper-service repo)
  - contract.ts now reads from config instead of hardcoded (GOOD change, keep this)
  - config.ts default was set to 0x8a3eb351... (WRONG — corrected locally to 0xc5810FC...)
  - keeper-service .env set to ORACLE_MODE=lazer (GOOD)
  - Both .env files were aligned (later re-corrected to 0xc5810FC...)
- Task 2: LIQUIDATION_KEEPER role verification — Role already granted, confirmed on-chain
- SUMMARY.md created, STATE.md and ROADMAP.md updated
- Planning docs committed (4bafdff in interface repo)

## Plan 25-02 (Wave 2) — E2E Liquidation Pipeline Proof
- Task 1: test-liquidation.ts created and committed (ddcb961)
- Task 2: Checkpoint active — multiple issues debugged:
  1. Pyth Lazer API key missing FX entitlements — FIXED by swapping to order-execution-keeper's token
  2. Order-execution-keeper using stale .env (not restarted) — user restarted
  3. InvalidOracleProvider revert — ROOT CAUSED: 0x8a3eb351 is NOT enabled on-chain

## Session Fixes (uncommitted)
- keeper-service/.env: PYTH_LAZER_FEED_PROVIDER_ADDRESS changed to 0xc5810FC...
- keeper-service/src/config.ts: default changed to 0xc5810FC...
- order-execution-keeper-service/.env: reverted to 0xc5810FC...
- keeper-service/.env: PYTH_PRO_ACCESS_TOKEN swapped to FX-entitled token (QpxMy21O...)
</completed_work>

<remaining_work>

## Immediate (Plan 25-02 Task 2 checkpoint)
1. Restart BOTH keepers with corrected .env values (0xc5810FC... for PythLazerFeedProvider)
2. Re-run `cd e2e && npx tsx test-liquidation.ts` to create undercollateralized position
3. Observe keeper-service scanner detecting and executing liquidation
4. Verify PostgreSQL records (candidate EXECUTED, execution MINED)
5. Verify on Basescan

## Post-checkpoint
- Complete Plan 25-02 SUMMARY.md, STATE.md, ROADMAP.md updates
- Run phase verification (gsd-verifier)
- Fix the committed config.ts default (0x8a3eb351 in e319e36 commit needs correction)
- Commit all .env-adjacent config fixes

## test-liquidation.ts bug (low priority)
- waitForExecution has stale event detection — matches old cancellation events for new orders
- Only matters if first size tier fails and it tries subsequent tiers
- Not blocking if the order succeeds on first attempt with correct oracle provider
</remaining_work>

<decisions_made>

- PythLazerFeedProvider address is definitively `0xc5810FC1932e44866bD0D041FbfB08d8AC2A67d6` — confirmed via on-chain DataStore queries:
  - `isOracleProviderEnabled(0xc5810FC...) = true`
  - `oracleProviderForToken(WETH) = 0xc5810FC...`
  - `oracleProviderForToken(USDC) = 0xc5810FC...`
  - `isOracleProviderEnabled(0x8a3eb351...) = false` (has stored prices but NOT enabled)
- Both keepers should use same PYTH_PRO_ACCESS_TOKEN (QpxMy21O...) — the keeper-service token (ZeJb1TSz...) lacks FX entitlements
- ORACLE_MODE=lazer is correct for keeper-service (confirmed in plan)
- contract.ts reading from config (not hardcoded) is a good architectural change — keep it
</decisions_made>

<blockers>
- None — root cause identified, .env files corrected, just needs restart and re-test
</blockers>

<context>
The execute-phase workflow is mid-Wave 2. The orchestrator spawned a gsd-executor for Plan 25-02 which hit the Task 2 checkpoint (human-verify). During the checkpoint, we discovered the wrong oracle provider address through iterative debugging:
1. First: FX API key crash → swapped token
2. Second: stored prices too stale → expected, need order execution to push fresh prices
3. Third: orders cancelled with same old cancellation TX → stale event detection bug in test script
4. Fourth: order reverted with InvalidOracleProvider → traced with `cast run`, found 0x8a3eb351 not enabled
5. Fifth: queried DataStore with correct key format → 0xc5810FC is the real provider

The execute-phase workflow state: Plan 25-01 SUMMARY exists, Plan 25-02 SUMMARY does not yet exist. Wave 2 executor agent was at checkpoint and has not been continued/completed.
</context>

<next_action>
1. Restart order-execution-keeper: `cd /Users/ken/Projects/0xM/order-execution-keeper-service && npm run dev`
2. Restart keeper-service: `cd /Users/ken/Projects/0xM/keeper-service && npm run dev`
3. Run test: `cd /Users/ken/Projects/0xM/0xMarkets-Interface/e2e && npx tsx test-liquidation.ts`
4. If position created successfully, observe keeper-service logs for liquidation detection
5. When pipeline verified, continue the execute-phase workflow (complete Plan 25-02, run verifier)
6. Fix the committed config.ts default address in keeper-service repo
</next_action>
