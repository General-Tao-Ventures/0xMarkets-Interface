---
phase: 18-event-detection-and-toast-feedback
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-02-26T18:25:02.104Z
---

<current_state>
Phase 18 execution is at Wave 2 (plan 18-02, Task 2 checkpoint). Plans 18-01 and 18-03 are complete. Plan 18-02 Task 1 (debug logging) is complete. Task 2 is a human-verify checkpoint for live toast lifecycle testing.

The frontend polling code is verified correct through extensive debugging. Phase A (receipt detection) works — finds DepositCreated from TX receipts. Phase B (execution polling via eth_getLogs) works — query returns 0 logs correctly when no DepositExecuted event exists on-chain.

The BLOCKER is the keeper service, not the frontend. The keeper has a massive backlog causing 16+ minute delays to execute new deposits, exceeding the frontend's 5-minute timeout. When the keeper DOES execute (confirmed via keeper logs showing "tx confirmed" for the test deposit key at block 38180964), the DepositExecuted event IS emitted on-chain — but by then the frontend has timed out.
</current_state>

<completed_work>

- Plan 18-01: Event Detection Polling Fallback - DONE (prior session)
- Plan 18-03: Gap Closure (wire watchOrderTxn + fix deps) - DONE
  - Task 1: Wired watchOrderTxn into createDepositTxn and createWithdrawalTxn (commit a5cb079)
  - Task 2: Removed status objects from useExecutionPolling useEffect deps (commit 3599a8b)
  - SUMMARY.md created (commit c60abed)
- Plan 18-02 Task 1: Added console.warn debug logging to useExecutionPolling (commit d9ed2db)
- Plan 18-02 Task 2: Human-verify checkpoint IN PROGRESS
  - Added detailed diagnostic logging to pollForEvents function (not committed yet)
  - Confirmed Phase A receipt detection works (DepositCreated parsed from TX receipt)
  - Confirmed Phase B eth_getLogs query is correct (returns 0 when event doesn't exist)
  - Confirmed keeper IS executing deposits (just slowly due to backlog)
  - Keeper restarted (PID 91235) but still has backlog from old unexecuted operations
</completed_work>

<remaining_work>

- Plan 18-02 Task 2: Need to re-test when keeper backlog is cleared
  - The keeper needs to clear its backlog so new deposits execute within seconds, not 16 minutes
  - Once keeper executes promptly, re-test deposit/withdrawal/order toast lifecycle
  - If pollForEvents finds the DepositExecuted event, toast should update to "Executed"
  - Also test withdrawal and order flows
- After 18-02 passes: phase verification, roadmap update
- Remove diagnostic console.warn logging from pollForEvents after UAT passes (or keep the existing debug logging from 18-02 Task 1 which was committed)
</remaining_work>

<decisions_made>

- Frontend polling architecture is correct — Phase A (receipt watching) and Phase B (eth_getLogs for execution events) work as designed
- The topic filter `[validTopics, eventNameHashes]` in pollForEvents is correct per the EventEmitter ABI
- The key matching logic in pollForEvents accesses ethers v6 decoded struct correctly (bytes32Items.items[].key/value)
- Root cause of UAT failures is keeper backlog latency, not frontend code
- Keeper was restarted (killed PID 57321, new PID 91235) but still needs backlog to clear
</decisions_made>

<blockers>
- Keeper backlog: The order-execution-keeper-service (port 37018) has accumulated ~1595 transient errors vs 867 confirmations. Nonce management issues cause many operations to fail and retry. New deposits get queued behind old retries, causing 16+ minute execution delays.
- To unblock: Either (1) wait for keeper to clear backlog naturally, (2) restart keeper with a mechanism to skip old/stale operations, or (3) manually execute the test deposit via the DepositHandler contract
</blockers>

<context>
The execute-phase workflow spawned the 18-02 executor agent which reached the checkpoint. The checkpoint agent (ID: a022184a7c1a90bc6) completed Task 1 and is waiting for human verification at Task 2.

When resuming, do NOT re-run the executor agent. Instead:
1. Clear the keeper backlog or restart it fresh
2. Submit a new deposit from the UI
3. Watch console for "[execution-polling] pollForEvents: got N logs" where N > 0
4. If N > 0, the toast should update — mark checkpoint as approved
5. If still 0 after 30s with keeper confirmed executing, investigate the eth_getLogs topic filter

The uncommitted change in useExecutionPolling.ts adds diagnostic logging to pollForEvents (log count, event names, key matching). This should be reverted or cleaned up before final commit.

Dev server should be running at localhost:3010 (may need restart: `cd /Users/ken/Projects/0xM/0xMarkets-Interface && yarn dev`).
</context>

<next_action>
Start with: Check keeper backlog status (`grep "tx confirmed" /tmp/keeper.log | tail -5` and `grep "transient error" /tmp/keeper.log | wc -l`). If keeper is still backlogged, restart it and wait for it to clear. Then submit a fresh deposit and verify the toast lifecycle completes. The frontend code is ready — the blocker is purely keeper execution speed.
</next_action>
