---
phase: 07-public-deployment
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - /Users/ken/Projects/0xM/docker-compose.yml
  - /Users/ken/Projects/0xM/order-execution-keeper-service/src/core/executors/orderExecutor.ts
  - /Users/ken/Projects/0xM/order-execution-keeper-service/src/core/scanners/orderScanner.ts
  - /Users/ken/Projects/0xM/order-execution-keeper-service/src/config.ts
  - /Users/ken/Projects/0xM/order-execution-keeper-service/.env
autonomous: false
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "A user can complete a full deposit-to-trade cycle with cloud keepers only (no local services)"
    - "Cloud keepers detect and execute orders within sub-60s timing"
    - "Cloud keeper PythLazerFeedProvider address matches the current on-chain provider"
    - "Cloud keeper order executor resolves the correct index token for oracle pricing"
    - "Cloud keeper scanner retries FAILED orders"
  artifacts:
    - path: "/Users/ken/Projects/0xM/docker-compose.yml"
      provides: "Updated PYTH_LAZER_FEED_PROVIDER_ADDRESS for both services"
      contains: "0x8a3eb351aDb32A813FCb53C418E8E09dd39E2D05"
    - path: "/Users/ken/Projects/0xM/order-execution-keeper-service/src/core/executors/orderExecutor.ts"
      provides: "Fixed token resolution using reader.getMarket() for index token"
      contains: "indexToken"
    - path: "/Users/ken/Projects/0xM/order-execution-keeper-service/src/core/scanners/orderScanner.ts"
      provides: "FAILED status included in pending order query"
      contains: "FAILED"
  key_links:
    - from: "docker-compose.yml"
      to: "keeper-service + order-execution-keeper containers"
      via: "PYTH_LAZER_FEED_PROVIDER_ADDRESS env var"
      pattern: "0x8a3eb351"
    - from: "orderExecutor.ts"
      to: "Reader contract"
      via: "reader.getMarket() call for index token resolution"
      pattern: "getMarket|indexToken"
    - from: "orderScanner.ts"
      to: "orderExecutor.ts"
      via: "FAILED orders passed to executor for retry"
      pattern: "FAILED"
---

<objective>
Sync cloud keepers on DigitalOcean with all v1.1 verification fixes so the full trading loop works from the public Vercel URL without any local services running.

Purpose: Close the gap between local dev (which works) and cloud deployment (which has stale keeper configs).
Output: Updated docker-compose.yml and keeper source files committed, ready for user to SSH-deploy to DO.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-public-deployment/07-CONTEXT.md
@.planning/phases/07-public-deployment/07-01-SUMMARY.md
@.planning/phases/06-position-management/06-04-SUMMARY.md
@/Users/ken/Projects/0xM/docker-compose.yml
@/Users/ken/Projects/0xM/order-execution-keeper-service/src/core/executors/orderExecutor.ts
@/Users/ken/Projects/0xM/order-execution-keeper-service/src/core/scanners/orderScanner.ts
@/Users/ken/Projects/0xM/order-execution-keeper-service/src/config.ts
@/Users/ken/Projects/0xM/order-execution-keeper-service/.env
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply v1.1 verification fixes to keeper configs and source</name>
  <files>
    /Users/ken/Projects/0xM/docker-compose.yml
    /Users/ken/Projects/0xM/order-execution-keeper-service/src/config.ts
    /Users/ken/Projects/0xM/order-execution-keeper-service/.env
    /Users/ken/Projects/0xM/order-execution-keeper-service/src/core/executors/orderExecutor.ts
    /Users/ken/Projects/0xM/order-execution-keeper-service/src/core/scanners/orderScanner.ts
  </files>
  <action>
    Four fixes identified during v1.1 verification (phase 06-04-SUMMARY.md) need to be confirmed present in the source files that will be deployed to DO. Check each file and apply if not already present:

    **Fix 1: PythLazerFeedProvider address (CRITICAL)**

    In `docker-compose.yml`, BOTH `keeper-service` and `order-execution-keeper` have:
    ```
    PYTH_LAZER_FEED_PROVIDER_ADDRESS: "0x93704d7C5E8CbB668c42Dd0a131d5A126244776e"
    ```
    This is the OLD address. Update both to the current v4 address:
    ```
    PYTH_LAZER_FEED_PROVIDER_ADDRESS: "0x8a3eb351aDb32A813FCb53C418E8E09dd39E2D05"
    ```

    Also check `order-execution-keeper-service/.env` and `order-execution-keeper-service/src/config.ts` for the same old address and update if found.

    **Fix 2: Order executor token resolution**

    In `orderExecutor.ts`, verify the fix from 06-04 is present: the executor should resolve the market's `indexToken` via `reader.getMarket()` for oracle pricing, NOT pass the market token address directly. Read the file and confirm. If the fix is missing, apply it per the pattern described in 06-04-SUMMARY.md.

    **Fix 3: Scanner FAILED order retry**

    In `orderScanner.ts`, verify that `getPendingOrderKeys()` (or equivalent) includes FAILED status in its query filter, not just PENDING. If missing, add FAILED to the status filter so failed orders are retried.

    **Fix 4: REQUEST_EXPIRATION_TIME**

    This was increased to 3600s (1 hour) on-chain during v1.1 verification. No keeper code change needed — this is a DataStore config already set. Confirm this is documented in the summary.

    **Important:** Read each file FIRST before modifying. Some fixes may already be present in the local source (they were applied during v1.1 verification). The goal is to ensure the local source matches what was tested and verified, so that when deployed to DO via `git pull && docker compose up --build`, everything works.
  </action>
  <verify>
    1. `grep "0x8a3eb351" /Users/ken/Projects/0xM/docker-compose.yml` returns 2 matches (both services)
    2. `grep "0x93704d7C5E8CbB668c42Dd0a131d5A126244776e" /Users/ken/Projects/0xM/docker-compose.yml` returns 0 matches (old address removed)
    3. `grep -i "indexToken\|getMarket" /Users/ken/Projects/0xM/order-execution-keeper-service/src/core/executors/orderExecutor.ts` confirms token resolution fix
    4. `grep -i "FAILED" /Users/ken/Projects/0xM/order-execution-keeper-service/src/core/scanners/orderScanner.ts` confirms FAILED retry
  </verify>
  <done>
    All 4 v1.1 verification fixes confirmed present in local source files. docker-compose.yml updated with correct PythLazer provider address. Source ready for deployment to DO server.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy keepers to DO and verify end-to-end trading loop</name>
  <what-built>
    All v1.1 verification fixes have been applied to the local keeper source and docker-compose.yml. The frontend is deployed to Vercel (from Plan 07-01). What remains is deploying the keepers to the DO server and verifying the full trading loop works from the public URL.
  </what-built>
  <how-to-verify>
    **Step 1: Deploy keepers to DigitalOcean**

    SSH to the DO server and deploy:
    ```bash
    ssh root@142.93.203.222
    cd /opt/0xmarkets
    git pull   # Pull latest changes (all repos)
    docker compose up --build -d
    docker compose logs --tail 20 keeper-service
    docker compose logs --tail 20 order-execution-keeper
    ```

    Confirm both services start without errors. Look for:
    - keeper-service: "Listening on port 37017" or similar
    - order-execution-keeper: "Scanner started" or similar

    **Step 2: Verify keeper proxy from public URL**

    ```bash
    curl -s https://<your-vercel-url>/api/keeper/prices/tickers | head -c 200
    ```
    Should return JSON price data.

    **Step 3: Full trading loop test**

    From the public Vercel URL (or custom domain):
    1. Connect wallet (must have testnet mUSDC)
    2. Go to Pools page -> Buy GM on any market -> Confirm deposit
    3. Wait for keeper to execute deposit (should complete within 60s)
    4. Go to Trade page -> Open a Long position on ETH/USD
    5. Wait for keeper to execute the order (should complete within 60s)
    6. Close the position
    7. Wait for close order to execute

    All 3 operations (deposit, open, close) must execute via cloud keepers within 60s each.

    **Step 4: Verify from a fresh perspective**

    Open the URL in an incognito window (no wallet connected):
    - App loads without errors
    - Markets and prices display correctly
    - Pages are navigable (Pools, Trade, Dashboard)

    If keeper is down during test, the KeeperStatusBanner should appear.
  </how-to-verify>
  <resume-signal>Type "approved" if full trading loop works from public URL, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
1. `docker-compose.yml` contains correct PythLazer address for both services
2. Keeper source files contain all 4 v1.1 fixes
3. Cloud keepers running on DO server after deployment
4. Full trading loop (deposit -> trade -> close) works from public Vercel URL
5. Cloud keeper execution timing is sub-60s per operation
</verification>

<success_criteria>
- A user can complete a full deposit-to-trade cycle with cloud keepers only (no local services running)
- Cloud keepers detect and execute orders within sub-60s
- The public URL works for first-time visitors (app loads, prices display)
</success_criteria>

<output>
After completion, create `.planning/phases/07-public-deployment/07-02-SUMMARY.md`
</output>
