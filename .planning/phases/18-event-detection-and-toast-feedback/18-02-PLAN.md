---
phase: 18-event-detection-and-toast-feedback
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/context/SyntheticsEvents/useExecutionPolling.ts
autonomous: false
requirements: [FB-01, FB-02]

must_haves:
  truths:
    - "After user submits a deposit, a Pending toast appears immediately"
    - "After the keeper executes the deposit, the toast updates to Executed"
    - "After user submits a withdrawal, a Pending toast appears immediately"
    - "After the keeper executes the withdrawal, the toast updates to Executed"
    - "After user submits a market order, a Pending toast appears immediately"
    - "After the keeper executes the order, the toast updates to Executed"
  artifacts:
    - path: "src/context/SyntheticsEvents/useExecutionPolling.ts"
      provides: "Polling fallback hook (may need parameter tuning)"
  key_links:
    - from: "src/context/SyntheticsEvents/SyntheticsEventsProvider.tsx"
      to: "src/components/StatusNotification/GmStatusNotification.tsx"
      via: "useSyntheticsEvents().depositStatuses/withdrawalStatuses"
      pattern: "useSyntheticsEvents"
    - from: "src/context/SyntheticsEvents/SyntheticsEventsProvider.tsx"
      to: "src/components/StatusNotification/OrderStatusNotification.tsx"
      via: "useSyntheticsEvents().orderStatuses"
      pattern: "useSyntheticsEvents"
---

<objective>
Verify the full toast notification lifecycle for all operation types on the live testnet deployment, and fix any issues found during testing.

Purpose: Plan 18-01 added the polling fallback, but the existing WS-based system and the new polling need to be verified working together on the live Base Sepolia deployment. This plan tests all three operation types and fixes any issues.

Output: Verified toast lifecycle for deposits, withdrawals, and orders; any bug fixes committed.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-event-detection-and-toast-feedback/18-01-SUMMARY.md

@src/context/SyntheticsEvents/SyntheticsEventsProvider.tsx
@src/context/SyntheticsEvents/useExecutionPolling.ts
@src/components/StatusNotification/GmStatusNotification.tsx
@src/components/StatusNotification/OrderStatusNotification.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add console logging for event detection debugging</name>
  <files>src/context/SyntheticsEvents/useExecutionPolling.ts</files>
  <action>
Add temporary `console.warn` statements (allowed by linting rules) to the polling hook to aid live debugging:

1. When a stuck operation is identified: `console.warn("[execution-polling] Polling for stuck operation:", key, "type:", type, "elapsed:", elapsedMs, "ms")`
2. When an execution/cancellation event is found via polling: `console.warn("[execution-polling] Found event via RPC poll:", eventType, "key:", key, "txnHash:", txnHash)`
3. When a timeout occurs: `console.warn("[execution-polling] Operation timed out:", key, "after", elapsedMs, "ms")`

These help verify whether the polling fallback is actually firing and finding events during live testing.

Also verify: the polling hook does not fire when the WS event is received promptly (i.e., if `executedTxnHash` is set before `POLL_DELAY_MS`, the operation should not be considered "stuck").
  </action>
  <verify>TypeScript compiles: `cd /Users/ken/Projects/0xM/0xMarkets-Interface && npx tsc --noEmit --pretty 2>&1 | head -5`</verify>
  <done>Console logging added for debugging the polling fallback during live testing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify toast notification lifecycle for all operation types</name>
  <action>
Human verifies the full toast notification lifecycle works on live testnet.

The existing system already had:
- Toast appears on submission (setPendingDeposit/setPendingWithdrawal/setPendingOrder)
- WebSocket subscription for execution/cancellation events
- Toast components (GmStatusNotification, OrderStatusNotification) that update based on event statuses

Plan 18-01 added:
- RPC polling fallback (useExecutionPolling) that catches events missed by WebSocket
- Timeout mechanism (5 min) so toasts never stay stuck on "Pending..." forever
- Timeout-specific error messages in toast components

Steps:
1. Start local dev server: `cd /Users/ken/Projects/0xM/0xMarkets-Interface && yarn dev`
2. Open browser to local dev URL, connect wallet

Test A - Deposit (DET-01, FB-01, FB-02):
3. Go to Pools page, select a market (e.g., ETH/USD)
4. Submit a deposit of a small amount of USDC
5. Verify: A toast appears immediately showing "Buying GM..." with "Sending buy request" status
6. Wait for keeper execution (typically 5-30 seconds)
7. Verify: Toast updates to show "Buy request sent" then "Buy order executed"

Test B - Withdrawal (DET-02):
8. Go to Pools page, select the same market
9. Submit a withdrawal of the GM tokens just received
10. Verify: Toast shows "Selling GM..." then updates to "Sell order executed"

Test C - Market Order (DET-03):
11. Go to Trade page
12. Submit a market long on ETH with a small position
13. Verify: Toast shows order name with "Sending order request" then "Order executed"

Check browser console for "[execution-polling]" messages to confirm fallback behavior.
  </action>
  <verify>User confirms all three operation types show correct toast lifecycle</verify>
  <done>All three operation types (deposit, withdrawal, order) show Pending -> Executed toast lifecycle; no toast stuck permanently on Pending</done>
</task>

</tasks>

<verification>
1. Deposit toast lifecycle: Pending -> Executed (or timeout error after 5 min)
2. Withdrawal toast lifecycle: Pending -> Executed (or timeout error after 5 min)
3. Order toast lifecycle: Pending -> Executed (or timeout error after 5 min)
4. No toast stuck permanently on "Pending..."
5. Console shows polling activity only when WS events are delayed/missed
</verification>

<success_criteria>
- All three operation types (deposit, withdrawal, order) show correct toast notification lifecycle
- Polling fallback fires when WebSocket events are missed (visible in console)
- Timeout mechanism prevents infinite "Pending..." state
- User confirms toast behavior is acceptable
</success_criteria>

<output>
After completion, create `.planning/phases/18-event-detection-and-toast-feedback/18-02-SUMMARY.md`
</output>
