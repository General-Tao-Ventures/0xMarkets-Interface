---
phase: 35-trigger-order-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/test-trigger-orders.ts
autonomous: false
requirements:
  - TRIG-01
  - TRIG-02

must_haves:
  truths:
    - "Deployed keeper on 142.93.203.222 has fresh oracle prices (< 270s old)"
    - "Keeper wallet has sufficient ETH balance for gas"
    - "LimitIncrease order executes on-chain via the deployed keeper"
    - "StopLossDecrease order executes on-chain via the deployed keeper"
    - "LimitDecrease (take-profit) order executes on-chain via the deployed keeper"
  artifacts:
    - path: "e2e/test-trigger-orders.ts"
      provides: "Trigger order E2E test suite"
      contains: "testLimitIncreaseLong"
  key_links:
    - from: "e2e/test-trigger-orders.ts"
      to: "order-execution-keeper-service"
      via: "submits trigger orders on-chain, keeper detects and executes them"
      pattern: "waitForExecution.*Order.*TRIGGER_TIMEOUT_MS"
---

<objective>
Verify that trigger orders (limit increase, stop-loss, take-profit) execute successfully end-to-end on the live Base Sepolia testnet, with the deployed keeper infrastructure handling execution.

Purpose: The InvalidOrderPrices (0x0481a15a) root cause has been identified and resolved (oracle price scaling was correct; trigger margins were increased to 5% to account for stored price staleness). E2E tests pass 4/4 locally. This plan confirms the deployed keeper on 142.93.203.222 is healthy and actually executes trigger orders submitted by the E2E tests.

Output: Documented root cause confirmation, passing E2E test run against the live deployed keeper, and human-verified keeper logs showing execution.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-trigger-order-fix/35-CONTEXT.md
@.planning/continue-trigger-order-price-fix.md

@e2e/test-trigger-orders.ts
@e2e/config.ts
@e2e/helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose deployed keeper health and run trigger order E2E tests</name>
  <files>e2e/test-trigger-orders.ts</files>
  <action>
Check the deployed keeper infrastructure on 142.93.203.222 to confirm it can execute trigger orders. Perform these checks in order:

1. **Keeper wallet balance**: SSH to 142.93.203.222 and check the order-execution-keeper Docker container logs for any "insufficient funds" errors. Alternatively, use the e2e config to read the keeper wallet's ETH balance on-chain. The keeper wallet was funded with 0.01 ETH previously -- verify it still has > 0.001 ETH for gas.

2. **Oracle freshness**: Check the order-execution-keeper logs for recent Pyth Lazer messages. The oracle module (`oracle.ts`) logs "pyth lazer message received" for the first 3 messages on startup and "oracle started" when all 7 feeds are cached. If the keeper has been running, look for recent execution attempts or "trigger simulation failed" messages which indicate the oracle is alive but price conditions are not yet met.

3. **Run a single quick trigger test**: From the e2e directory, run `TEST=limit-increase npx tsx test-trigger-orders.ts` to submit a LimitIncrease order and wait for the keeper to execute it (90s timeout). This is the fastest way to confirm the full pipeline works.

4. **If the keeper oracle is stale (prices > 270s old)**: The keeper Docker container may need a restart. SSH to the server and run `docker restart order-execution-keeper` then wait 30s for oracle startup, then re-run the test.

5. **If the keeper wallet is out of ETH**: This is a blocker that requires the user to fund the wallet. Create a note describing the required amount and wallet address.

6. **If the single test passes**: Run the full suite `npx tsx test-trigger-orders.ts` (all 4 tests: LimitIncrease, StopLoss, TakeProfit, PendingStay).

Important context:
- The keeper checks trigger orders every ~15s (TRIGGER_SEEN_TTL_MS = 15_000 in executor.ts)
- The keeper simulates trigger orders before submitting to avoid wasting gas (simulateContract in executor.ts line 474)
- Tests use 5% price margins to account for stored price staleness vs live Pyth data
- The order-execution-keeper-service is a separate repo at /Users/ken/Projects/0xM/order-execution-keeper-service/
- SSH access: `ssh root@142.93.203.222`
- Docker commands: `docker logs order-execution-keeper --tail 50`, `docker restart order-execution-keeper`
  </action>
  <verify>
    <automated>cd /Users/ken/Projects/0xM/0xMarkets-Interface/e2e && npx tsx test-trigger-orders.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 trigger order tests pass (LimitIncrease, StopLoss, TakeProfit, PendingStay) with the deployed keeper executing orders on-chain. Exit code 0 with "4/4 PASSED" in output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of trigger order execution</name>
  <action>
Verify the trigger order E2E results and optionally inspect keeper logs.

What was built: Verified trigger order execution end-to-end against the live Base Sepolia testnet. The deployed keeper on 142.93.203.222 detects and executes LimitIncrease, StopLossDecrease, and LimitDecrease (take-profit) orders submitted by the E2E test suite.

How to verify:
1. Review the E2E test output above -- all 4 tests should show PASS
2. Optionally SSH to 142.93.203.222 and check keeper logs: `docker logs order-execution-keeper --tail 100`
   - Look for "tx confirmed" entries showing the keeper executed the trigger orders
   - Look for "enqueued (priority)" entries showing the keeper detected the orders
3. Confirm the root cause summary: InvalidOrderPrices was from stale stored prices vs live oracle; 5% trigger margins in tests account for this naturally

Resume: Type "approved" if trigger orders are executing correctly, or describe any issues observed.
  </action>
  <verify>User confirms trigger orders execute correctly on live testnet</verify>
  <done>User has approved that all 3 trigger order types (LimitIncrease, StopLossDecrease, LimitDecrease) execute via the deployed keeper.</done>
</task>

</tasks>

<verification>
- E2E test suite exits with code 0
- All 4 test cases report PASS status
- Keeper logs on 142.93.203.222 show successful trigger order execution transactions
- No InvalidOrderPrices (0x0481a15a) errors in keeper logs
</verification>

<success_criteria>
- LimitIncrease, StopLossDecrease, and LimitDecrease orders all execute on-chain via the deployed keeper (TRIG-02)
- Root cause of InvalidOrderPrices documented: oracle price staleness > 300s MAX_ORACLE_PRICE_AGE, resolved by ensuring keeper oracle WebSocket stays connected with 270s cache TTL (TRIG-01)
- E2E test suite passes 4/4 against live testnet
</success_criteria>

<output>
After completion, create `.planning/phases/35-trigger-order-fix/35-01-SUMMARY.md`
</output>
