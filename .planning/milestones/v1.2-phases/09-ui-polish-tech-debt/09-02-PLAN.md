---
phase: 09-ui-polish-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/types/positions.ts
  - sdk/src/modules/positions/positions.ts
  - sdk/src/utils/positions.ts
  - sdk/src/utils/fees/priceImpact.ts
  - sdk/src/utils/trade/decrease.ts
  - sdk/src/utils/trade/increase.ts
  - src/domain/synthetics/positions/usePositions.ts
  - src/domain/synthetics/positions/usePositionsInfo.ts
  - src/domain/synthetics/positions/utils.ts
  - src/domain/synthetics/orders/useOrders.ts
  - src/domain/multichain/fetchMultichainTokenBalances.spec.ts
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-03
  - DEBT-04

must_haves:
  truths:
    - "TypeScript build completes without errors (tsc --noEmit passes)"
    - "SDK test suite is clean — all tests pass or have explicit skip annotations with reasons"
    - "pendingImpactAmount workaround is properly resolved (removed or fixed)"
    - "Keeper execution efficiency has been investigated with findings documented"
  artifacts:
    - path: "sdk/src/types/positions.ts"
      provides: "Clean PositionInfo type without pendingImpactAmount workaround"
      contains: "PositionInfo"
    - path: "src/domain/synthetics/orders/useOrders.ts"
      provides: "TypeScript-clean orders hook"
    - path: "src/domain/multichain/fetchMultichainTokenBalances.spec.ts"
      provides: "Fixed or skipped multichain balance test"
  key_links:
    - from: "sdk/src/types/positions.ts"
      to: "src/domain/synthetics/positions/usePositionsInfo.ts"
      via: "PositionInfo type import"
      pattern: "import.*PositionInfo"
    - from: "src/domain/synthetics/orders/useOrders.ts"
      to: "tsc --noEmit"
      via: "clean compilation"
      pattern: "OrderInfoStructOutput"
---

<objective>
Resolve tech debt items — pendingImpactAmount workaround, TypeScript errors, SDK test failures, and keeper efficiency investigation.

Purpose: The codebase should have no unresolved workarounds blocking future development, clean builds, and clean test output.
Output: Clean tsc build, clean test suite, resolved pendingImpactAmount, keeper efficiency findings.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-polish-tech-debt/09-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve pendingImpactAmount workaround and useOrders.ts TypeScript error</name>
  <files>
    sdk/src/types/positions.ts
    sdk/src/modules/positions/positions.ts
    sdk/src/utils/positions.ts
    sdk/src/utils/fees/priceImpact.ts
    sdk/src/utils/trade/decrease.ts
    sdk/src/utils/trade/increase.ts
    src/domain/synthetics/positions/usePositions.ts
    src/domain/synthetics/positions/usePositionsInfo.ts
    src/domain/synthetics/positions/utils.ts
    src/domain/synthetics/orders/useOrders.ts
    src/context/SyntheticsStateContext/selectors/tradeboxSelectors/selectTradeboxSidecarOrders.ts
    src/components/PositionEditor/hooks/usePositionEditorData.tsx
    src/pages/LeaderboardPage/components/LeaderboardPositionsTable.tsx
    src/domain/synthetics/testUtils/mocks.ts
  </files>
  <action>
    Two tech debt items that touch the SDK type system:

    **1. pendingImpactAmount (DEBT-01):**
    Trace the full usage of `pendingImpactAmount` across the codebase (23 files reference it). The field is currently defaulted to `0n` because the on-chain contract struct doesn't include it. Decide the resolution:
    - If no code path actually uses the value meaningfully beyond passing `0n` through: REMOVE the field entirely from `PositionInfo` type in `sdk/src/types/positions.ts`, remove from all construction sites in `sdk/src/modules/positions/positions.ts` and `sdk/src/utils/positions.ts`, and update all consumers to not reference it.
    - If some calculation depends on it (check `sdk/src/utils/fees/priceImpact.ts`, `sdk/src/utils/trade/decrease.ts`, `sdk/src/utils/trade/increase.ts`): document what the correct mapping should be, and either fix the mapping or add a clear `// TODO: pendingImpactAmount not available on-chain, defaults to 0n — affects [specific calculation]` comment at the definition site.
    Per user decision: Claude traces usage and decides — fix, remove, or document.

    **2. useOrders.ts TypeScript error (DEBT-03):**
    Open `src/domain/synthetics/orders/useOrders.ts` and find the `OrderInfoStructOutput` type mismatch. The STATE.md notes this as a "pre-existing TypeScript error" but current `tsc --noEmit` passes clean. Verify the error is truly resolved. If it still exists (maybe suppressed), fix the type import/export mismatch. If already clean, confirm and move on.

    Run `npx tsc --noEmit` after changes to confirm no regressions.
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "tsc clean"</automated>
    <manual>Grep for pendingImpactAmount — should either be fully removed or have clear documentation at definition site.</manual>
  </verify>
  <done>pendingImpactAmount is either removed from the type system or documented with clear rationale. useOrders.ts compiles cleanly. `tsc --noEmit` passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix or skip SDK test failures and investigate keeper efficiency</name>
  <files>
    src/domain/multichain/fetchMultichainTokenBalances.spec.ts
    src/domain/synthetics/__tests__/trade/decrease.spec.ts
    src/domain/synthetics/trade/utils/decrease.spec.ts
    sdk/src/utils/__tests__/positions.spec.ts
  </files>
  <action>
    Two remaining tech debt items:

    **1. SDK test failures (DEBT-02):**
    Current state: `npx vitest run` shows 1 failing test (`fetchMultichainTokenBalances.spec.ts` — TypeError reading undefined chain result). The STATE.md previously noted "17 pre-existing failing SDK test files (21 tests)" but current run shows only 1 failure.

    For the failing test in `src/domain/multichain/fetchMultichainTokenBalances.spec.ts`:
    - The test calls a real RPC endpoint for chain `SOURCE_BASE_MAINNET` which returns undefined. This is a network-dependent integration test.
    - Fix approach: Skip the test with a documented reason. Add `it.skip("should fetch real token balances — skipped: requires live RPC to Base Mainnet which may be unreachable in CI", ...)` or fix the test to handle the case where the chain response is undefined.
    - Per user decision: "mix approach — fix easy ones, skip hard ones with documented reasons."

    Verify all other tests pass. Run full `npx vitest run` and confirm clean output (all pass or explicitly skipped).

    **2. Keeper execution efficiency (DEBT-04):**
    Investigate keeper execution timing for demo readiness. This does NOT require code changes in this repo — it's an investigation task.

    Steps:
    a. Read the keeper-service and order-execution-keeper-service health endpoints to understand current execution timing.
    b. Check the keeper scan cycle intervals in the codebase at `/Users/ken/Projects/0xM/order-execution-keeper-service/src/` and `/Users/ken/Projects/0xM/keeper-service/src/` — look for scan interval configs, sleep timers, and batch sizes.
    c. Document findings: What is the current scan interval? What is the typical execution latency from order creation to execution? Are there obvious optimization opportunities (reduce scan interval, batch oracle updates, parallel execution)?
    d. Record findings in the SUMMARY. If there are quick wins (e.g., reducing a sleep timer from 10s to 3s), note them but do NOT make changes to keeper repos in this plan — those are separate services.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
    <manual>Verify test output shows 0 failures. All tests either pass or are explicitly skipped with documented reasons.</manual>
  </verify>
  <done>All SDK/app tests pass or are skipped with documented reasons (0 failures in test output). Keeper efficiency investigation findings documented in SUMMARY with specific metrics and optimization recommendations.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vitest run` shows 0 failures (all pass or explicitly skipped)
- pendingImpactAmount resolved (removed or documented)
- Keeper efficiency findings documented
</verification>

<success_criteria>
- The TypeScript build completes without errors (useOrders.ts error resolved)
- SDK test suite output is clean — all tests either pass or have explicit skip annotations with documented reasons
- pendingImpactAmount workaround is properly resolved
- Keeper execution efficiency is investigated with actionable findings
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-polish-tech-debt/09-02-SUMMARY.md`
</output>
