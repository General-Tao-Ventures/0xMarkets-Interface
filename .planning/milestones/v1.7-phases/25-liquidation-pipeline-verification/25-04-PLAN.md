---
phase: 25-liquidation-pipeline-verification
plan: 04
type: execute
wave: 2
depends_on: [25-03]
files_modified:
  - e2e/test-liquidation.ts
autonomous: false
gap_closure: true
requirements: [LIQ-03, LIQ-04]

must_haves:
  truths:
    - "A successful executeLiquidation transaction exists on Base Sepolia Basescan from keeper wallet 0x48Cb0d738C9B3F44F60f7338F788fa093FD25828"
    - "PostgreSQL liquidation_executions table has at least one row with status=MINED and a valid tx_hash"
    - "PostgreSQL liquidation_candidates table has at least one row with status=EXECUTED"
    - "The confirmator processed an OrderExecuted event with orderType=7 and updated both execution and candidate records"
  artifacts:
    - path: "e2e/test-liquidation.ts"
      provides: "Script to create undercollateralized position (may need size tier adjustments for pool liquidity)"
  key_links:
    - from: "keeper-service/src/core/executor.ts"
      to: "LiquidationHandler.executeLiquidation()"
      via: "writeContract call"
      pattern: "executeLiquidation"
    - from: "keeper-service/src/core/confirmator.ts"
      to: "keeper-service/src/core/store.ts"
      via: "updateExecutionStatus(MINED) + updateCandidateStatus(EXECUTED)"
      pattern: "updateExecutionStatus.*MINED"
---

<objective>
Complete the end-to-end liquidation pipeline by executing a real liquidation on Base Sepolia -- proving the executor submits a successful transaction and the confirmator records the result in PostgreSQL.

Purpose: Verification found that Gaps 2 and 3 (LIQ-03 and LIQ-04) were never satisfied because testnet pool reserves were exhausted during the original 25-02 execution. The pipeline code is verified correct (9 bug fixes applied), but the write path (executor TX submission and confirmator status updates) was never triggered with real data. This plan closes both gaps by ensuring pool liquidity exists, creating a liquidatable position, and running the full pipeline.

Output: Basescan transaction hash proving executeLiquidation succeeded, PostgreSQL records showing MINED execution and EXECUTED candidate.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-liquidation-pipeline-verification/25-RESEARCH.md
@.planning/phases/25-liquidation-pipeline-verification/25-02-SUMMARY.md
@.planning/phases/25-liquidation-pipeline-verification/25-VERIFICATION.md

<interfaces>
<!-- Pipeline status from 25-02-SUMMARY -->

Verified components (from 25-02):
- Scanner: account discovery, Lazer pricing, liquidatability check, DB persistence, cooldown -- ALL VERIFIED
- Executor: oracle param building, gas estimation gate -- VERIFIED
- Confirmator: event watcher running, watching for orderType=7 -- RUNNING

Unverified components (the gaps this plan closes):
- Executor: TX submission (writeContract.executeLiquidation) -- NEVER TRIGGERED
- Confirmator: updateExecutionStatus(MINED) + updateCandidateStatus(EXECUTED) -- NEVER TRIGGERED

Root cause: InsufficientReserveForOpenInterest on WETH/USD pool prevented new position creation during 25-02.

From e2e/test-liquidation.ts (existing):
- Creates LONG position on WETH/USD with configurable size tiers
- Uses ensureApprovals + multicall pattern from test-orders.ts
- Currently tries size tiers: $500 at 50x, $200 at 20x, $100 at 10x, $50 at 5x

Pool liquidity requirement:
- WETH/USD pool needs open interest reserves to accept new positions
- If pool is still exhausted, user must add liquidity via Buy GM flow before running this plan
- Alternatively, test on a different market (WBTC/USD) if WETH/USD pool remains blocked

Database status flow:
- LiquidationCandidate: NEW -> PENDING -> EXECUTED
- LiquidationExecution: SUBMITTED -> MINED

Keeper wallet: 0x48Cb0d738C9B3F44F60f7338F788fa093FD25828
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify pool liquidity and adjust test-liquidation.ts if needed</name>
  <files>
    e2e/test-liquidation.ts
  </files>
  <action>
**Step 1: Check current pool state.**

Query the WETH/USD pool's open interest reserves to determine if new positions can be created:
```bash
# Check position count in DataStore
cast call 0x3B9d71B497aD2d3c32a7c24e96565f84a58089a7 \
  "getBytes32Count(bytes32)(uint256)" \
  $(cast keccak $(cast abi-encode "f(string)" "POSITION_LIST")) \
  --rpc-url https://sepolia.base.org
```

**Step 2: Attempt to create an undercollateralized position.**

Run the existing test-liquidation.ts:
```bash
cd /Users/ken/Projects/0xM/0xMarkets-Interface/e2e && npx tsx test-liquidation.ts
```

**If position creation succeeds:** Proceed to Task 2.

**If InsufficientReserveForOpenInterest:** The pool needs liquidity. Try the following in order:

a) **Try smaller size tiers.** Edit test-liquidation.ts to add smaller tiers like $20 at 2x leverage. Even a tiny position that gets liquidated proves the pipeline works.

b) **Try a different market.** If WETH/USD is completely blocked, modify test-liquidation.ts to use WBTC/USD market:
   - Market: Check `e2e/config.ts` MARKETS object for WBTC/USD market address
   - Same order pattern, different market address and index token

c) **Try SHORT instead of LONG.** The pool may have reserves on the short side. Change `isLong: false` and set `acceptablePrice: 0n` (accept any price downward).

d) **If all fail:** The user must add pool liquidity first. Log a clear message: "All markets exhausted. Please add liquidity to a pool via the Buy GM flow before retrying."

**Step 3: Verify the position was created and is liquidatable.**

After position creation, check that the keeper-service scanner would detect it. The position should have very high leverage (near or past the liquidation threshold). If the position is NOT immediately liquidatable:
- The order-execution-keeper must be running and pushing prices
- A small adverse price move may be needed
- The gas estimation gate in the executor validates against on-chain Lazer prices

**Important:** Do not modify the core scanner/executor/confirmator code. Only adjust test-liquidation.ts parameters (size tiers, market, direction) to work around testnet pool constraints.
  </action>
  <verify>
    <automated>
cd /Users/ken/Projects/0xM/0xMarkets-Interface/e2e && npx tsc --noEmit test-liquidation.ts 2>&1 | head -10
    </automated>
  </verify>
  <done>
- test-liquidation.ts compiles without TypeScript errors
- A position has been created on Base Sepolia that is liquidatable or very close to liquidation threshold
- The position's market, collateral, and leverage are logged for cross-reference with keeper-service output
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run full liquidation pipeline and verify executor TX + confirmator status</name>
  <what-built>
This checkpoint verifies the final two pipeline components that were never triggered during 25-02: the executor's TX submission and the confirmator's status updates.

**Pre-conditions (verify before proceeding):**
1. Plan 25-03 is complete (correct PythLazerFeedProvider address in all files)
2. order-execution-keeper-service is running (pushes prices to PythLazerFeedProvider):
   ```bash
   curl -s http://localhost:37018/health 2>/dev/null || echo "NOT RUNNING"
   ```
3. A liquidatable position exists on-chain (from Task 1)

**Automated steps the executor will perform:**

1. **Start keeper-service:**
   ```bash
   cd /Users/ken/Projects/0xM/keeper-service && npm run dev
   ```

2. **Watch logs for the full pipeline cycle.** Expected log sequence:
   - `"starting scan cycle"` -- scan begins
   - `"discovered accounts with positions"` -- account discovery
   - `"found liquidatable position"` -- scanner detects position
   - `"executing liquidation"` -- executor starts
   - `"transaction submitted"` with `txHash` -- ON-CHAIN TX SUBMITTED (Gap 2 closure)
   - `"confirmed liquidation execution"` -- confirmator processes event (Gap 3 closure)

3. **If executor says "PositionShouldNotBeLiquidated" in gas estimation:**
   The position is not liquidatable at current Lazer prices. Options:
   - Wait for price movement (position may become liquidatable as price moves)
   - The scanner cooldown (5 min) will retry later
   - If stuck, close position and recreate with higher leverage

4. **Verify PostgreSQL records:**
   ```bash
   cd /Users/ken/Projects/0xM/keeper-service && npx prisma studio
   ```
   Or via psql:
   ```bash
   psql $DATABASE_URL -c "SELECT id, status, tx_hash, submitted_at FROM liquidation_executions WHERE status='MINED' ORDER BY submitted_at DESC LIMIT 5;"
   psql $DATABASE_URL -c "SELECT id, status, detected_at FROM liquidation_candidates WHERE status='EXECUTED' ORDER BY detected_at DESC LIMIT 5;"
   ```

5. **Verify on Basescan:**
   Take the `tx_hash` from the execution record and verify at `https://sepolia.basescan.org/tx/{HASH}`:
   - Transaction status: Success
   - Called function: executeLiquidation
   - From: 0x48Cb0d738C9B3F44F60f7338F788fa093FD25828 (keeper wallet)
  </what-built>
  <how-to-verify>
1. **Executor TX submitted:** Keeper logs show "transaction submitted" with a valid txHash (not a revert or gas estimation failure)
2. **Transaction succeeded on Basescan:** Navigate to `https://sepolia.basescan.org/tx/{HASH}` and confirm Status: Success
3. **Confirmator processed event:** Keeper logs show "confirmed liquidation execution" with matching executionId
4. **Database records:**
   - `liquidation_executions` has at least one row with `status=MINED` and a non-null `tx_hash`
   - `liquidation_candidates` has at least one row with `status=EXECUTED`
5. **Cross-reference:** The tx_hash in the database matches the Basescan transaction

If the pipeline runs but executor TX fails:
- Check if it's a role error (LIQUIDATION_KEEPER not granted) -- should not happen per LIQ-01 verification
- Check if it's a price mismatch (Lazer vs stored prices diverge too much)
- Check for nonce conflicts (both keepers sharing wallet)

If pool liquidity is STILL insufficient after Task 1 adjustments:
- Report this as an external blocker
- Document what was tried and what's needed (user must add pool liquidity)
  </how-to-verify>
  <resume-signal>Type "approved" with the Basescan tx hash if the full pipeline completed, or describe which step failed and the error output</resume-signal>
</task>

</tasks>

<verification>
1. Basescan shows a successful `executeLiquidation` transaction from keeper wallet `0x48Cb0d738C9B3F44F60f7338F788fa093FD25828`
2. PostgreSQL `liquidation_executions` has a row with `status=MINED` and matching `tx_hash`
3. PostgreSQL `liquidation_candidates` has a row with `status=EXECUTED`
4. Keeper-service logs show the complete pipeline: scan -> detect -> execute -> confirm
</verification>

<success_criteria>
- The executor submitted executeLiquidation and the transaction succeeded on-chain (Basescan proof)
- The confirmator detected the OrderExecuted event and updated execution status to MINED
- The confirmator updated the candidate status to EXECUTED
- LIQ-03 and LIQ-04 are now satisfied with on-chain evidence
</success_criteria>

<output>
After completion, create `.planning/phases/25-liquidation-pipeline-verification/25-04-SUMMARY.md`
</output>
