---
phase: 07-public-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/keeper.ts
  - src/components/StatusNotification/GmStatusNotification.tsx
  - src/components/KeeperStatusBanner/KeeperStatusBanner.tsx
  - src/App/App.tsx
  - .planning/PROJECT.md
autonomous: true
requirements: [DEPLOY-01]

must_haves:
  truths:
    - "Visiting the Vercel URL loads the app without errors for a first-time visitor"
    - "The keeper proxy API route forwards requests to the cloud keeper and returns valid price data"
    - "Custom domain resolves to the Vercel deployment"
    - "If the keeper is unreachable, a banner informs the user instead of a broken page"
  artifacts:
    - path: "api/keeper.ts"
      provides: "Vercel serverless proxy to cloud keeper (env-driven URL)"
      contains: "process.env.KEEPER_URL"
    - path: "src/components/KeeperStatusBanner/KeeperStatusBanner.tsx"
      provides: "Banner component shown when keeper is unreachable"
      min_lines: 20
  key_links:
    - from: "api/keeper.ts"
      to: "http://142.93.203.222:37017"
      via: "KEEPER_URL environment variable"
      pattern: "process\\.env\\.KEEPER_URL"
    - from: "sdk/src/configs/oracleKeeper.ts"
      to: "api/keeper.ts"
      via: "/api/keeper relative URL"
      pattern: "/api/keeper"

user_setup:
  - service: vercel
    why: "Deploy frontend to production"
    env_vars:
      - name: KEEPER_URL
        source: "Cloud keeper IP — use http://142.93.203.222:37017"
    dashboard_config:
      - task: "Connect custom domain"
        location: "Vercel Dashboard -> Project -> Settings -> Domains -> Add domain"
---

<objective>
Deploy the 0xMarkets Interface to Vercel so anyone with the URL can access the app, configure the keeper proxy with environment variables instead of hardcoded IPs, connect the custom domain, and add a keeper-down banner for resilience.

Purpose: Make the app publicly accessible — the first step toward a demo-ready product.
Output: Live Vercel deployment at custom domain, environment-driven keeper proxy, keeper-down banner.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-public-deployment/07-CONTEXT.md
@api/keeper.ts
@vercel.json
@vite.config.ts
@sdk/src/configs/oracleKeeper.ts
@src/components/StatusNotification/GmStatusNotification.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment-driven keeper proxy and keeper-down banner</name>
  <files>
    api/keeper.ts
    src/components/StatusNotification/GmStatusNotification.tsx
    src/components/KeeperStatusBanner/KeeperStatusBanner.tsx
    src/components/KeeperStatusBanner/KeeperStatusBanner.scss
    src/App/App.tsx
    .planning/PROJECT.md
  </files>
  <action>
    **1. Update api/keeper.ts to use environment variable instead of hardcoded IP:**

    Replace `const KEEPER_URL = "http://142.93.203.222:37017";` with:
    ```typescript
    const KEEPER_URL = process.env.KEEPER_URL || "http://142.93.203.222:37017";
    ```
    This makes the proxy configurable via Vercel environment variables while keeping the hardcoded fallback for zero-config local dev.

    **2. Update GmStatusNotification.tsx to use relative URL for order-execution-keeper API:**

    The `KEEPER_API_URL` at line 59 is hardcoded to `http://142.93.203.222:37018`. This will fail from a Vercel deployment due to mixed-content (HTTPS page calling HTTP IP). For now, add a Vercel serverless proxy route for the order-execution-keeper too, OR make the URL environment-driven.

    Best approach: Add a second Vercel serverless proxy at `api/order-keeper.ts` that proxies to `process.env.ORDER_KEEPER_URL || "http://142.93.203.222:37018"`, and update GmStatusNotification.tsx to use `/api/order-keeper` as the base URL instead of the hardcoded IP. Add to vercel.json a rewrite rule:
    ```json
    {
      "source": "/api/order-keeper/:path*",
      "destination": "/api/order-keeper?_path=:path*"
    }
    ```
    Also add the function config for `api/order-keeper.ts`.

    Update vite.config.ts to add a dev proxy for `/api/order-keeper`:
    ```typescript
    "/api/order-keeper": {
      target: "http://142.93.203.222:37018",
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api\/order-keeper/, ""),
    },
    ```

    **3. Create KeeperStatusBanner component:**

    Create `src/components/KeeperStatusBanner/KeeperStatusBanner.tsx` — a simple banner that:
    - On mount, fetches `/api/keeper/prices/tickers` (the same endpoint the app uses for price data)
    - If fetch fails or returns non-200, shows a yellow/amber banner at the top of the page: "System maintenance in progress. You can browse markets but trading operations may be delayed."
    - If fetch succeeds, renders nothing
    - Re-checks every 60 seconds
    - Use a simple `useState` + `useEffect` pattern with `setInterval`
    - Style with a new SCSS module file: amber background (#FFA726), white text, centered, padding 8px 16px, font-size 14px

    **4. Add KeeperStatusBanner to App.tsx:**

    Import and render `<KeeperStatusBanner />` near the top of the App component render tree, above the main content but below any provider wrappers. It should appear on every page.

    **5. Update PROJECT.md:**

    In the Context section, change "managed by Michael Wallert" or similar to "managed by user directly via SSH" per the correction noted in 07-CONTEXT.md. The keeper infrastructure line should read: "Two services on DigitalOcean (142.93.203.222), managed directly via SSH and Docker Compose at `/opt/0xmarkets/`"
  </action>
  <verify>
    1. `yarn build` succeeds (no TypeScript/import errors from new files)
    2. `grep -r "142.93.203.222" src/` returns NO results (all hardcoded IPs removed from frontend source)
    3. `grep "process.env.KEEPER_URL" api/keeper.ts` confirms env variable usage
    4. `grep "process.env.ORDER_KEEPER_URL" api/order-keeper.ts` confirms env variable usage
    5. `grep "Michael Wallert" .planning/PROJECT.md` returns nothing
  </verify>
  <done>
    All hardcoded keeper IPs removed from frontend source code. Keeper proxy uses environment variables. KeeperStatusBanner component exists and renders conditionally. PROJECT.md corrected. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel and configure production environment</name>
  <files>
    vercel.json
  </files>
  <action>
    **1. Verify vercel.json has all needed rewrites and function configs:**

    Ensure vercel.json includes:
    - Rewrite for `/api/keeper/:path*` (already exists)
    - Rewrite for `/api/order-keeper/:path*` (added in Task 1)
    - Function configs for both `api/keeper.ts` and `api/order-keeper.ts`
    - Framework detection should auto-detect Vite

    **2. Deploy to Vercel via CLI:**

    Run `vercel --yes` from the project root to create a preview deployment. This will:
    - Use the existing linked project (prj_7YJzSjoybqdaCUw66W5zXw5vxy7u)
    - Build with Vite
    - Deploy serverless functions from `api/`

    **3. Set environment variables on Vercel:**

    ```bash
    vercel env add KEEPER_URL production <<< "http://142.93.203.222:37017"
    vercel env add ORDER_KEEPER_URL production <<< "http://142.93.203.222:37018"
    ```

    If the CLI prompts for environment selection, set for Production only.

    **4. Deploy to production:**

    ```bash
    vercel --prod --yes
    ```

    **5. Verify the deployment loads:**

    ```bash
    curl -s -o /dev/null -w "%{http_code}" https://0xmarkets-interface.vercel.app/
    ```
    (Use the actual Vercel URL from the deploy output)

    Verify the keeper proxy works:
    ```bash
    curl -s https://<vercel-url>/api/keeper/prices/tickers | head -c 200
    ```

    **6. Connect custom domain:**

    If the user's custom domain is ready:
    ```bash
    vercel domains add <domain> --yes
    ```
    Otherwise, note this as a follow-up for the user to complete via Vercel Dashboard.

    Note: The Vercel project is already linked (`.vercel/project.json` exists with projectId and orgId).
  </action>
  <verify>
    1. `vercel ls` shows the production deployment
    2. `curl -s -o /dev/null -w "%{http_code}" <vercel-url>` returns 200
    3. `curl -s <vercel-url>/api/keeper/prices/tickers` returns JSON price data (not 502)
    4. Page loads in browser without console errors
  </verify>
  <done>
    Vercel production deployment is live. The public URL loads the app. The keeper proxy returns price data. Environment variables are set for production.
  </done>
</task>

</tasks>

<verification>
1. Visit the Vercel URL in an incognito browser — app loads, markets display, prices update
2. `/api/keeper/prices/tickers` returns valid JSON from the cloud keeper
3. `/api/order-keeper/api/health` returns 200 (if order keeper health endpoint exists)
4. No hardcoded 142.93.203.222 IPs remain in `src/` directory
5. Build passes: `yarn build` exits 0
</verification>

<success_criteria>
- The Vercel URL loads the app for a first-time visitor without errors
- Keeper proxy forwards requests using environment variables
- Keeper-down banner appears when keeper is unreachable, hides when healthy
- Custom domain connected (or user has clear instructions to do so)
</success_criteria>

<output>
After completion, create `.planning/phases/07-public-deployment/07-01-SUMMARY.md`
</output>
