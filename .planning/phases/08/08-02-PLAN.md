---
phase: 08-keeper-monitoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/ken/Projects/0xM/keeper-service/src/utils/logger.ts
  - /Users/ken/Projects/0xM/keeper-service/src/utils/healthState.ts
  - /Users/ken/Projects/0xM/keeper-service/src/server/httpServer.ts
  - /Users/ken/Projects/0xM/keeper-service/src/index.ts
  - /Users/ken/Projects/0xM/keeper-service/src/config.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/scanner.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/executor.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/confirmator.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/oracle.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/pythLazerOracle.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/pythOracle.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/positionFetcher.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/paramsWatcher.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/candleCollector.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/preview.ts
  - /Users/ken/Projects/0xM/keeper-service/src/core/store.ts
  - /Users/ken/Projects/0xM/keeper-service/src/server/controllers/positionsController.ts
  - /Users/ken/Projects/0xM/keeper-service/src/server/controllers/executionsController.ts
  - /Users/ken/Projects/0xM/keeper-service/src/server/controllers/candidatesController.ts
  - /Users/ken/Projects/0xM/keeper-service/src/server/controllers/candlesController.ts
  - /Users/ken/Projects/0xM/keeper-service/src/server/controllers/statsController.ts
  - /Users/ken/Projects/0xM/keeper-service/src/server/controllers/pricesController.ts
  - /Users/ken/Projects/0xM/keeper-service/src/server/controllers/incentivesController.ts
  - /Users/ken/Projects/0xM/keeper-service/src/utils/envInfo.ts
  - /Users/ken/Projects/0xM/keeper-service/package.json
autonomous: true
requirements: [MON-01, MON-02]

must_haves:
  truths:
    - "GET /health returns 200 with lastPricePush, candleStatus, wsStatus when keeper is running normally"
    - "GET /health returns 503 when last scan cycle completed more than 2 minutes ago"
    - "All keeper log output is structured JSON with timestamp, level, service, event fields"
    - "No console.log/warn/error calls remain in production source files"
  artifacts:
    - path: "/Users/ken/Projects/0xM/keeper-service/src/utils/logger.ts"
      provides: "pino logger instance configured for JSON output with service=keeper-service"
      exports: ["logger"]
    - path: "/Users/ken/Projects/0xM/keeper-service/src/utils/healthState.ts"
      provides: "Mutable health state singleton tracking lastScanTime, lastPricePush, candleStatus, wsStatus"
      exports: ["healthState", "recordScan", "recordPricePush", "setCandleStatus", "setWsStatus"]
    - path: "/Users/ken/Projects/0xM/keeper-service/src/server/httpServer.ts"
      provides: "GET /health endpoint returning real state with 200/503 based on 2-minute threshold"
      contains: "healthState"
  key_links:
    - from: "/Users/ken/Projects/0xM/keeper-service/src/core/scanner.ts"
      to: "healthState"
      via: "recordScan() called after each scan cycle completes"
      pattern: "recordScan"
    - from: "/Users/ken/Projects/0xM/keeper-service/src/server/httpServer.ts"
      to: "healthState"
      via: "reads lastScanTime to compute 200 vs 503"
      pattern: "healthState"
    - from: "all src files"
      to: "logger"
      via: "import { logger } from utils/logger.js replaces all console.log/warn/error"
      pattern: "import.*logger.*from.*utils/logger"
---

<objective>
Add structured logging (pino) and a real health check endpoint to the keeper-service.

Purpose: Replace all ~138 console.log/warn/error calls with pino JSON logging, and upgrade the existing /health endpoint from static `{"status":"ok"}` to a real health check that reports last price push, candle generation status, and WebSocket connection — returning 503 when the keeper is stalled.

Output: keeper-service with pino logger, health state tracking, and all console.log calls replaced.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08/08-CONTEXT.md

Source files (read before modifying):
@/Users/ken/Projects/0xM/keeper-service/src/index.ts
@/Users/ken/Projects/0xM/keeper-service/src/config.ts
@/Users/ken/Projects/0xM/keeper-service/src/server/httpServer.ts
@/Users/ken/Projects/0xM/keeper-service/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pino logger, health state module, and upgrade /health endpoint</name>
  <files>
    /Users/ken/Projects/0xM/keeper-service/package.json
    /Users/ken/Projects/0xM/keeper-service/src/utils/logger.ts
    /Users/ken/Projects/0xM/keeper-service/src/utils/healthState.ts
    /Users/ken/Projects/0xM/keeper-service/src/server/httpServer.ts
    /Users/ken/Projects/0xM/keeper-service/src/index.ts
  </files>
  <action>
    1. Install pino: `cd /Users/ken/Projects/0xM/keeper-service && pnpm add pino && pnpm add -D @types/pino`

    2. Create `/Users/ken/Projects/0xM/keeper-service/src/utils/logger.ts`:
       - Import pino
       - Create and export a single logger instance: `pino({ name: "keeper-service" })`
       - pino outputs JSON by default with timestamp, level, msg — no extra config needed
       - Do NOT use pino-pretty in production (JSON lines only, per CONTEXT.md)

    3. Create `/Users/ken/Projects/0xM/keeper-service/src/utils/healthState.ts`:
       - Export a singleton object `healthState` with:
         - `startedAt: Date` (set at module load time)
         - `lastScanTime: Date | null` (null until first scan completes — this is the primary liveness signal)
         - `lastPricePush: Date | null` (null until first price push)
         - `candleCollectorActive: boolean` (default false)
         - `wsConnected: boolean` (default false)
       - Export `recordScan()` — updates lastScanTime to now
       - Export `recordPricePush()` — updates lastPricePush to now
       - Export `setCandleStatus(active: boolean)` — updates candleCollectorActive
       - Export `setWsStatus(connected: boolean)` — updates wsConnected
       - NOTE: keeper-service's scan interval is 30s (config), so 2-minute threshold = ~4 missed scans before unhealthy.

    4. Update `/Users/ken/Projects/0xM/keeper-service/src/server/httpServer.ts`:
       - Import `healthState` from `../utils/healthState.js`
       - Import `logger` from `../utils/logger.js`
       - Replace the existing `/health` handler with:
         ```
         const HEALTH_THRESHOLD_MS = 2 * 60 * 1000; // 2 minutes
         const now = Date.now();
         const lastScan = healthState.lastScanTime;
         const isHealthy = lastScan !== null && (now - lastScan.getTime()) < HEALTH_THRESHOLD_MS;
         const status = isHealthy ? "healthy" : "unhealthy";
         const statusCode = isHealthy ? 200 : 503;
         res.status(statusCode).json({
           status,
           service: "keeper-service",
           uptime: Math.floor((now - healthState.startedAt.getTime()) / 1000),
           lastScanTime: lastScan?.toISOString() ?? null,
           lastPricePush: healthState.lastPricePush?.toISOString() ?? null,
           candleCollectorActive: healthState.candleCollectorActive,
           wsConnected: healthState.wsConnected,
         });
         ```
       - Replace console.log in listen callback with logger.info

    5. Update `/Users/ken/Projects/0xM/keeper-service/src/index.ts`:
       - Import `logger` from `./utils/logger.js`
       - Import health state functions from `./utils/healthState.js`
       - Replace ALL console.log/warn/error calls with logger equivalents
       - Remove `[keeper-service]` prefix tags from messages (pino's `name` field handles this)
       - After Pyth Lazer Oracle connects, call `setWsStatus(true)`
       - After `startCandleCollector()` call, call `setCandleStatus(true)`. In the shutdown handler, call `setCandleStatus(false)`
       - The scanner.ts handles its own scan loop — health state integration for `recordScan()` will be done in Task 2 when scanner.ts is migrated
  </action>
  <verify>
    Run `cd /Users/ken/Projects/0xM/keeper-service && pnpm build` — must compile without errors.
    Verify no console.log in src/index.ts: `grep -c "console\." src/index.ts` should return 0.
  </verify>
  <done>
    - pino installed in package.json
    - logger.ts exports a pino instance with name "keeper-service"
    - healthState.ts tracks lastScanTime, lastPricePush, candleCollectorActive, wsConnected
    - /health returns 200 with real state when healthy, 503 when stalled
    - src/index.ts uses logger and healthState instead of console.log
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all remaining console.log calls to pino logger</name>
  <files>
    /Users/ken/Projects/0xM/keeper-service/src/config.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/scanner.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/executor.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/confirmator.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/oracle.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/pythLazerOracle.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/pythOracle.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/positionFetcher.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/paramsWatcher.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/candleCollector.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/preview.ts
    /Users/ken/Projects/0xM/keeper-service/src/core/store.ts
    /Users/ken/Projects/0xM/keeper-service/src/server/controllers/positionsController.ts
    /Users/ken/Projects/0xM/keeper-service/src/server/controllers/executionsController.ts
    /Users/ken/Projects/0xM/keeper-service/src/server/controllers/candidatesController.ts
    /Users/ken/Projects/0xM/keeper-service/src/server/controllers/candlesController.ts
    /Users/ken/Projects/0xM/keeper-service/src/server/controllers/statsController.ts
    /Users/ken/Projects/0xM/keeper-service/src/server/controllers/pricesController.ts
    /Users/ken/Projects/0xM/keeper-service/src/server/controllers/incentivesController.ts
    /Users/ken/Projects/0xM/keeper-service/src/utils/envInfo.ts
  </files>
  <action>
    For EVERY file listed, read it first, then:

    1. Add `import { logger } from "../utils/logger.js"` (adjust relative path per file depth — for core/ files it's `../utils/logger.js`, for server/controllers/ it's `../../utils/logger.js`)
    2. Create a child logger at file scope: `const log = logger.child({ module: "scanner" })` (use the filename without extension as the module name)
    3. Replace every `console.log(...)` with `log.info(...)`
    4. Replace every `console.warn(...)` with `log.warn(...)`
    5. Replace every `console.error(...)` with `log.error(...)`
    6. When the original passes an Error object, use pino's error serialization: `log.error({ err: error }, "message")`
    7. When the original includes structured data (keys, addresses, counts, txHashes, durations), extract them into the first-arg object: `log.info({ key, gasUsed }, "deposit executed")` instead of string interpolation
    8. Remove bracketed prefixes like `[Scanner]`, `[Executor]`, `[Confirmator]` etc from messages — the child logger's `module` field provides this context

    SPECIAL CASES:

    **scanner.ts (23 console calls, most in the codebase):**
    - This file has the main scan loop. After each complete scan cycle, call `recordScan()` from healthState. Import it: `import { recordScan } from "../utils/healthState.js"`
    - Place the `recordScan()` call at the end of the scan function, after all position checks complete, in a finally block or after the try/catch so it runs even if some positions fail

    **pythLazerOracle.ts (23 console calls):**
    - This file currently accepts a `console` parameter for its logger. Change the `createPythLazerOracle` function to accept a pino Logger instance instead of `console`. Update the function body to use logger.info/warn/error instead of console.log/warn/error.
    - Update the caller in `src/index.ts` to pass `logger.child({ module: "pythLazerOracle" })` instead of `console`
    - When Pyth Lazer WebSocket connects/disconnects, call `setWsStatus(true)` / `setWsStatus(false)` from healthState

    **executor.ts (19 console calls):**
    - Contains price push logic. After each successful price/liquidation execution, call `recordPricePush()` from healthState

    **candleCollector.ts (3 console calls):**
    - After candle collector starts its interval, call `setCandleStatus(true)`. On stop, call `setCandleStatus(false)`.

    After all files are migrated, verify:
    - `grep -r "console\.\(log\|warn\|error\)" /Users/ken/Projects/0xM/keeper-service/src/ --include="*.ts" | grep -v "node_modules" | grep -v ".test." | grep -v "test/"` — should return ZERO lines (excluding test files)
  </action>
  <verify>
    1. `cd /Users/ken/Projects/0xM/keeper-service && pnpm build` — compiles without errors
    2. `grep -r "console\.\(log\|warn\|error\)" src/ --include="*.ts" | grep -v test | grep -v ".test."` — returns 0 matches
  </verify>
  <done>
    - All ~138 console.log/warn/error calls in production source files replaced with pino logger
    - Each file uses a child logger with { module: "filename" } for context
    - Error objects passed via { err: error } pattern for proper serialization
    - Structured data (keys, addresses, counts) in first-arg object, not string interpolation
    - scanner.ts calls recordScan() after each scan cycle
    - executor.ts calls recordPricePush() after successful executions
    - pythLazerOracle.ts calls setWsStatus() on connect/disconnect
    - candleCollector.ts calls setCandleStatus() on start/stop
    - TypeScript builds without errors
    - Zero console.log calls remain in non-test source files
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/ken/Projects/0xM/keeper-service && pnpm build` succeeds
2. `grep -rc "console\.\(log\|warn\|error\)" src/ --include="*.ts" | grep -v ":0$" | grep -v test` returns nothing
3. Health endpoint logic: review httpServer.ts for 200/503 status code logic with 2-minute threshold
4. healthState tracks: lastScanTime, lastPricePush, candleCollectorActive, wsConnected, startedAt
</verification>

<success_criteria>
- pino is the sole logging mechanism in keeper-service (no console.log in production code)
- GET /health returns structured JSON with real service state and correct 200/503 status codes
- Health state is updated by scanner (recordScan), executor (recordPricePush), pythLazerOracle (setWsStatus), candleCollector (setCandleStatus)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08/08-02-SUMMARY.md`
</output>
