---
phase: 27-liquidation-pipeline-e2e-execution
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-02-28T21:30:00.000Z
---

<current_state>
Phase 27 plan 27-01: Tasks 1-2 complete (committed). Task 3 scanner price fix DONE (commit bff78a4), but positions are NOT liquidatable at current market prices. WETH has recovered above entry, so the 43x long and 21x short positions are healthy.

Pipeline fully verified through gas-estimation stage — just short of an actual on-chain liquidation TX.
</current_state>

<completed_work>

- Task 1: Seed WETH/USD pool with 10,000 USDC — Done (commit e37578c82)
- Task 2: Create high-leverage positions — Done (commit 4e2d1a541)
- Task 3: Scanner price fix — Done (commit bff78a4)
  - Replaced updatePrice write txs with getOraclePrice view calls (no gas, no nonce issues)
  - ORACLE_PROVIDER_ADDRESS (0xc5810FC) successfully decodes WS data for WETH/WBTC/USDC
  - Synthetics (EUR/GBP/JPY/GOLD) fall back to getStoredPrice (stale but valid)
  - Added Multicall3 address to chain definition — fixes account discovery
  - Result: 8 positions from 2 accounts, fresh decoded prices, accurate liquidatability checks
</completed_work>

<remaining_work>

- Sub-problem 1: ✅ Scanner price decoding works (getOraclePrice view calls)
- Sub-problem 2: ❌ Positions NOT liquidatable at current prices (WETH recovered)
  - Need to either create new extreme-leverage positions OR wait for price movement
- Sub-problem 3: ❌ No liquidation TX to verify (blocked by sub-problem 2)
- Sub-problem 4: ❌ Create SUMMARY.md and update STATE.md/ROADMAP.md
</remaining_work>

<decisions_made>

- Replaced updatePrice write transactions with getOraclePrice view calls — eliminates "Missing or invalid parameters" errors for crypto tokens and avoids gas/nonce issues entirely
- ORACLE_PROVIDER_ADDRESS (0xc5810FC) is the working provider for getOraclePrice decoding, not PYTH_LAZER_FEED_PROVIDER_ADDRESS (0x8a3eb351) which reverts with 0x2b6e7c3f
- Added Multicall3 (0xcA11bde05977b3631167028862bE2a173976CA11) to chain definition in contract.ts
- Synthetics fail getOraclePrice on both providers — "best ask price" issue in Pyth feed data. Fall back to stored prices.
- Stored prices use GMX precision 10^(30-decimals): WETH ~$2,061 (62.5h stale), valid but stale

</decisions_made>

<blockers>

- **Positions not liquidatable**: WETH has recovered above entry price. The 43x long position (entry ~$2,100) has ~4.9x remaining/min ratio — healthy. No positions are underwater at current market prices.
- **Synthetic getOraclePrice**: EUR/GBP/JPY/GOLD Pyth Lazer WS data fails getOraclePrice on both providers. Falls back to stale stored prices. Not blocking for WETH/USD liquidation testing.

</blockers>

<pipeline_verification>
What's been verified:
✅ Pyth Lazer WS connected and streaming binary data for all 7 feeds
✅ getOraclePrice decodes WETH/WBTC/USDC from WS data via ORACLE_PROVIDER_ADDRESS
✅ Multicall3 account discovery finds 2 accounts with 8 positions
✅ Reader.isPositionLiquidatable evaluates correctly with fresh prices
✅ Scanner creates candidate + signed decision when position appears liquidatable
✅ Executor builds oracle params with inline Lazer data from WS cache
✅ Executor gas-estimates executeLiquidation (correctly rejects healthy positions)
✅ Cooldown mechanism triggers after PositionShouldNotBeLiquidated
NOT tested: Actual successful liquidation TX on-chain (no position is underwater)
</pipeline_verification>

<next_action>
Pool reserve is completely exhausted ($0 available) — InsufficientReserve error on ALL size/direction combos (8 attempts, $3k-$8k, long+short). Fresh wallet approach confirmed working (funding, minting, approvals, order creation all succeed). The pool's existing 8 positions consume 100% of reserve capacity.

RECOMMENDED: Accept pipeline verification as sufficient. Write SUMMARY.md and complete v1.7.
ALTERNATIVE: Seed significantly more pool liquidity (>$20k) to free reserve, then retry position creation.
</next_action>
