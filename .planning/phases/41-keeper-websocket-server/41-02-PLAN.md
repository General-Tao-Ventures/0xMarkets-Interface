---
phase: 41-keeper-websocket-server
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified: []
autonomous: false
requirements: [KWS-01, KWS-02, KWS-03, KWS-04]

must_haves:
  truths:
    - "WebSocket client can connect to wss://keeper.0xmarkets.io through Cloudflare proxy"
    - "Connected client receives ticker messages with price data"
    - "Connected client receives candle messages at ~200ms intervals"
    - "Connection stays alive through Cloudflare's idle timeout via server heartbeat pings"
  artifacts: []
  key_links:
    - from: "Cloudflare proxy (keeper.0xmarkets.io)"
      to: "keeper-service port 37017"
      via: "WebSocket upgrade through Cloudflare Flexible SSL"
      pattern: "wss://keeper.0xmarkets.io"
---

<objective>
Deploy the WebSocket broadcast server to the DO droplet and verify end-to-end connectivity through Cloudflare.

Purpose: Confirm that the WebSocket server works in production with TLS termination, Cloudflare proxy, and live Pyth price data -- not just in unit tests.
Output: Live WebSocket endpoint at wss://keeper.0xmarkets.io broadcasting ticker and candle data.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-keeper-websocket-server/41-01-SUMMARY.md
@.planning/phases/40-infrastructure-keeper-hermes-sse/40-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy updated keeper-service to DO droplet</name>
  <files></files>
  <action>
    Build the keeper-service with the new WebSocket broadcast code:
    ```bash
    cd /Users/ken/Projects/0xM/keeper-service && pnpm build
    ```

    Deploy to the DO droplet (142.93.203.222). The keeper-service runs in Docker on the droplet. Steps:
    1. SSH to droplet: `ssh root@142.93.203.222`
    2. Navigate to keeper-service directory
    3. Pull latest code (or copy built artifacts)
    4. Rebuild Docker image
    5. Restart the container

    If the deployment process is unclear from existing docs, check `keeper-service/Dockerfile` and `keeper-service/docker-compose.yml` (or equivalent) for the deployment pattern. Also check `keeper-service/docs/cloudflare-setup.md` for infrastructure context.

    After restart, verify the health endpoint still works:
    ```bash
    curl -s https://keeper.0xmarkets.io/health | jq .
    ```
    Confirm the response includes `wsBroadcastActive: true` and `wsClients: 0`.
  </action>
  <verify>
    <automated>curl -sf https://keeper.0xmarkets.io/health | grep -q "wsBroadcastActive"</automated>
  </verify>
  <done>Keeper-service deployed with WebSocket broadcast active, health endpoint confirms wsBroadcastActive: true</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify WebSocket connectivity through Cloudflare</name>
  <action>
    WebSocket broadcast server deployed to DO droplet, accessible through Cloudflare TLS proxy at wss://keeper.0xmarkets.io.

    Verification steps for user:
    1. Connect with wscat:
       ```bash
       npx wscat -c wss://keeper.0xmarkets.io
       ```
    2. Verify you receive JSON messages with `"type": "ticker"` containing price data for WETH, WBTC, EUR, GBP, JPY, GOLD, USDC
    3. Verify you receive JSON messages with `"type": "candle"` containing OHLC data at ~200ms intervals
    4. Let the connection stay open for 2+ minutes to confirm heartbeat keeps it alive through Cloudflare's idle timeout
    5. Check health endpoint shows wsClients: 1 while connected:
       ```bash
       curl -s https://keeper.0xmarkets.io/health | jq .wsClients
       ```
    6. Disconnect and reconnect to verify connection lifecycle works cleanly
  </action>
  <verify>User confirms ticker and candle messages flow correctly through wss://keeper.0xmarkets.io</verify>
  <done>User approved: WebSocket endpoint works end-to-end through Cloudflare with ticker and candle streaming</done>
</task>

</tasks>

<verification>
1. Health endpoint: `curl -s https://keeper.0xmarkets.io/health` shows wsBroadcastActive: true
2. WebSocket connection: `npx wscat -c wss://keeper.0xmarkets.io` receives ticker and candle messages
3. Connection survives 2+ minutes (heartbeat working through Cloudflare)
</verification>

<success_criteria>
- wscat connects to wss://keeper.0xmarkets.io and receives continuous ticker/candle JSON messages
- Connection stays alive for 2+ minutes without dropping
- Health endpoint reports active WebSocket broadcast and client count
- Disconnecting and reconnecting works cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/41-keeper-websocket-server/41-02-SUMMARY.md`
</output>
