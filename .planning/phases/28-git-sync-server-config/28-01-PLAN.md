---
phase: 28-git-sync-server-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/ken/Projects/0xM/docker-compose.yml
autonomous: true
requirements: [GIT-01, GIT-02, CFG-01, CFG-02, CFG-03]

must_haves:
  truths:
    - "All 16 keeper-service commits from v1.7 are visible on GitHub"
    - "All 63 frontend commits are visible on GitHub (Vercel auto-deploy triggered)"
    - "docker-compose.yml contains correct v1.7 contract addresses for both keeper services"
    - "docker-compose.yml sets ORACLE_MODE=lazer for keeper-service"
    - "docker-compose.yml includes ORACLE_PROVIDER_ADDRESS for keeper-service liquidation executor"
  artifacts:
    - path: "/Users/ken/Projects/0xM/docker-compose.yml"
      provides: "Production docker-compose with correct v1.7 addresses and oracle config"
      contains: "ORACLE_MODE: lazer"
  key_links:
    - from: "docker-compose.yml keeper-service environment"
      to: "keeper-service/src/config.ts"
      via: "environment variable names match config.ts expectations"
      pattern: "ORACLE_PROVIDER_ADDRESS.*0xc5810"
    - from: "docker-compose.yml order-execution-keeper environment"
      to: "order-execution-keeper-service/src/config.ts"
      via: "environment variable names match config.ts expectations"
      pattern: "ORDER_HANDLER_ADDRESS.*0x63dE8c"
---

<objective>
Push all local commits to GitHub and update docker-compose.yml with correct v1.7 contract addresses and oracle configuration.

Purpose: Get all code on GitHub so the server can pull it, and ensure the production docker-compose has correct addresses so containers will connect to the right contracts when rebuilt.
Output: All three repos pushed to GitHub, docker-compose.yml updated and committed.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key contract addresses (from v1.7 Phase 24 redeployment and Phase 25 oracle fixes):

Current correct addresses (from local .env files and sdk/configs):
- DataStore: 0x3B9d71B497aD2d3c32a7c24e96565f84a58089a7
- EventEmitter: 0xd5aAfa71f745645Db84cB4877873701ddAf2514c
- Reader: 0x1e6Ca8042e7BC258BBbA35C5C86F013b4eceC03C
- LiquidationHandler: 0x241829af5Fd67bf67F0c9226Ce4907dd87A94cA8
- ReferralStorage: 0xF5F9CdBe6225aBFF7cE2F290d12bc1BaCCC926E2
- DepositHandler: 0xA91306c067959C157Df04f8c07568Ce51146484c
- WithdrawalHandler: 0x6b2aDac8313AA5971143Cc8dDd1cf7057163B68C
- OrderHandler: 0x63dE8c596687EA9C752a9b7548Bc02360d3d04Ad
- ExchangeRouter: 0xF98622Ff9Dfd6bC7877EB0653cbE1bA7dCC54321
- PythLazerFeedProvider (newer, getStoredPrice): 0x8a3eb351aDb32A813FCb53C418E8E09dd39E2D05
- OracleProvider (DataStore-registered, for execution): 0xc5810FC1932e44866bD0D041FbfB08d8AC2A67d6

<interfaces>
From keeper-service/src/config.ts — env vars read:
  DATABASE_URL, RPC_URL, PRIVATE_KEY, PORT, CHAIN_ID,
  READER_ADDRESS, DATA_STORE_ADDRESS, LIQUIDATION_HANDLER_ADDRESS,
  EVENT_EMITTER_ADDRESS, REFERRAL_STORAGE_ADDRESS,
  PYTH_LAZER_FEED_PROVIDER_ADDRESS (default: 0x8a3eb351...),
  ORACLE_PROVIDER_ADDRESS (default: 0xc5810... — used for executeLiquidation),
  PYTH_PRO_ACCESS_TOKEN, ORACLE_MODE (default: hermes),
  SCAN_INTERVAL_SECONDS

From order-execution-keeper-service/src/config.ts — env vars read:
  RPC_URL (required), WS_RPC_URL (required), CHAIN_ID (required),
  PRIVATE_KEY (required), DATA_STORE_ADDRESS, READER_ADDRESS,
  EVENT_EMITTER_ADDRESS, DEPOSIT_HANDLER_ADDRESS,
  WITHDRAWAL_HANDLER_ADDRESS, ORDER_HANDLER_ADDRESS,
  PYTH_LAZER_FEED_PROVIDER_ADDRESS (required — used as oracle provider for execution),
  PYTH_PRO_ACCESS_TOKEN (required), PORT (default: 37018),
  FLASHBLOCKS_RPC_URL (optional)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push all local commits to GitHub</name>
  <files>None (git operations only)</files>
  <action>
Push local commits from all three repositories to their GitHub remotes:

1. **keeper-service** (16 unpushed commits on `ken/keeper-updates`):
   ```bash
   cd /Users/ken/Projects/0xM/keeper-service
   git push origin ken/keeper-updates
   ```
   Verify: `git log --oneline origin/ken/keeper-updates..HEAD` returns empty.

2. **Frontend interface** (63 unpushed commits on `ken/integration`):
   ```bash
   cd /Users/ken/Projects/0xM/0xMarkets-Interface
   git push origin ken/integration
   ```
   Verify: `git log --oneline origin/ken/integration..HEAD` returns empty.
   Note: This push triggers Vercel auto-deploy for the frontend.

3. **order-execution-keeper** — already fully pushed (0 unpushed commits on `ken/keeper-rebuild`). Verify this is still the case:
   ```bash
   cd /Users/ken/Projects/0xM/order-execution-keeper-service
   git log --oneline origin/ken/keeper-rebuild..HEAD
   ```
   Should return empty.

Do NOT push to main/master. Push only to the feature branches listed above.
  </action>
  <verify>
    <automated>cd /Users/ken/Projects/0xM/keeper-service && echo "keeper-service unpushed:" && git log --oneline origin/ken/keeper-updates..HEAD | wc -l && cd /Users/ken/Projects/0xM/0xMarkets-Interface && echo "frontend unpushed:" && git log --oneline origin/ken/integration..HEAD | wc -l && cd /Users/ken/Projects/0xM/order-execution-keeper-service && echo "order-keeper unpushed:" && git log --oneline origin/ken/keeper-rebuild..HEAD | wc -l</automated>
  </verify>
  <done>All three repos show 0 unpushed commits. GitHub has all v1.7 work.</done>
</task>

<task type="auto">
  <name>Task 2: Update docker-compose.yml with v1.7 addresses and oracle config</name>
  <files>/Users/ken/Projects/0xM/docker-compose.yml</files>
  <action>
Update the root docker-compose.yml at `/Users/ken/Projects/0xM/docker-compose.yml` with ALL correct v1.7 contract addresses and configuration.

**keeper-service environment changes:**
Replace ALL stale addresses with current correct addresses:
- READER_ADDRESS: "0x1e6Ca8042e7BC258BBbA35C5C86F013b4eceC03C"
- DATA_STORE_ADDRESS: "0x3B9d71B497aD2d3c32a7c24e96565f84a58089a7"
- LIQUIDATION_HANDLER_ADDRESS: "0x241829af5Fd67bf67F0c9226Ce4907dd87A94cA8"
- EVENT_EMITTER_ADDRESS: "0xd5aAfa71f745645Db84cB4877873701ddAf2514c"
- REFERRAL_STORAGE_ADDRESS: "0xF5F9CdBe6225aBFF7cE2F290d12bc1BaCCC926E2"
- PYTH_LAZER_FEED_PROVIDER_ADDRESS: "0x8a3eb351aDb32A813FCb53C418E8E09dd39E2D05"

Add NEW env vars that keeper-service now requires:
- ORACLE_PROVIDER_ADDRESS: "0xc5810FC1932e44866bD0D041FbfB08d8AC2A67d6" (DataStore-registered, used by liquidation executor)
- ORACLE_MODE: "lazer" (hardcoded, not from ${ORACLE_MODE:-hermes})

**order-execution-keeper environment changes:**
Replace ALL stale addresses:
- DATA_STORE_ADDRESS: "0x3B9d71B497aD2d3c32a7c24e96565f84a58089a7"
- READER_ADDRESS: "0x1e6Ca8042e7BC258BBbA35C5C86F013b4eceC03C"
- EVENT_EMITTER_ADDRESS: "0xd5aAfa71f745645Db84cB4877873701ddAf2514c"
- DEPOSIT_HANDLER_ADDRESS: "0xA91306c067959C157Df04f8c07568Ce51146484c"
- WITHDRAWAL_HANDLER_ADDRESS: "0x6b2aDac8313AA5971143Cc8dDd1cf7057163B68C"
- ORDER_HANDLER_ADDRESS: "0x63dE8c596687EA9C752a9b7548Bc02360d3d04Ad"
- PYTH_LAZER_FEED_PROVIDER_ADDRESS: "0xc5810FC1932e44866bD0D041FbfB08d8AC2A67d6" (registered oracle provider — order-execution-keeper uses this directly for execution, not the getStoredPrice address)

Add NEW env vars:
- FLASHBLOCKS_RPC_URL: ${FLASHBLOCKS_RPC_URL:-https://sepolia-preconf.base.org} (optional but improves TX speed)

**Important:** Keep the structure of the file (services, volumes, healthchecks, ports, depends_on) exactly the same. Only change the environment variable values and add new ones. Keep ${...} syntax for secrets (PRIVATE_KEY, POSTGRES_PASSWORD, etc.) — only hardcode addresses and config values that are the same across all environments.

Commit the updated file in the 0xM root or in keeper-service (whichever makes sense for the repo structure). The docker-compose.yml lives at /Users/ken/Projects/0xM/ which may not be a git repo — check first. If it's not a git repo, note this in the summary (the file is updated locally and will be rsync'd or the server will be converted to git in Plan 02).
  </action>
  <verify>
    <automated>grep -c "0x63dE8c596687EA9C752a9b7548Bc02360d3d04Ad" /Users/ken/Projects/0xM/docker-compose.yml && grep -c "ORACLE_MODE.*lazer" /Users/ken/Projects/0xM/docker-compose.yml && grep -c "ORACLE_PROVIDER_ADDRESS" /Users/ken/Projects/0xM/docker-compose.yml && grep -c "0x3B9d71B497aD2d3c32a7c24e96565f84a58089a7" /Users/ken/Projects/0xM/docker-compose.yml</automated>
  </verify>
  <done>docker-compose.yml contains all v1.7 contract addresses, ORACLE_MODE=lazer, ORACLE_PROVIDER_ADDRESS, and FLASHBLOCKS_RPC_URL. No stale pre-v1.7 addresses remain. Each grep returns at least 1 match.</done>
</task>

</tasks>

<verification>
1. `git log --oneline origin/ken/keeper-updates..HEAD` in keeper-service returns empty (0 unpushed)
2. `git log --oneline origin/ken/integration..HEAD` in frontend returns empty (0 unpushed)
3. `grep "0xBaD049d5" /Users/ken/Projects/0xM/docker-compose.yml` returns no matches (stale address gone)
4. `grep "0xb53122a7" /Users/ken/Projects/0xM/docker-compose.yml` returns no matches (stale address gone)
5. `grep "ORACLE_MODE.*lazer" /Users/ken/Projects/0xM/docker-compose.yml` returns a match
6. `grep "ORACLE_PROVIDER_ADDRESS" /Users/ken/Projects/0xM/docker-compose.yml` returns a match
</verification>

<success_criteria>
- All local commits from v1.7 work are on GitHub across all three repos
- docker-compose.yml has every contract address matching the current local .env files
- ORACLE_MODE is set to lazer (not hermes)
- ORACLE_PROVIDER_ADDRESS is present for keeper-service
- No stale pre-v1.7 addresses remain in docker-compose.yml
</success_criteria>

<output>
After completion, create `.planning/phases/28-git-sync-server-config/28-01-SUMMARY.md`
</output>
