---
phase: 06-position-management
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/PositionSeller/PositionSeller.tsx
  - src/components/PositionSeller/PositionSellerAdvancedDisplayRows.tsx
  - src/domain/synthetics/trade/usePositionSellerState.ts
  - src/domain/synthetics/trade/utils/validation.ts
  - src/components/PositionItem/PositionItem.tsx
  - src/components/PositionList/PositionList.tsx
autonomous: true
requirements:
  - POS-02

must_haves:
  truths:
    - "User can click Close on an open position and the PositionSeller modal opens"
    - "User can close a full position and receive collateral back in their wallet"
    - "User can enter a partial close amount and reduce position size by that amount"
    - "After closing, the position disappears (full close) or updates (partial close) in the positions list"
  artifacts:
    - path: "src/components/PositionSeller/PositionSeller.tsx"
      provides: "Position close modal with full and partial close support"
    - path: "src/domain/synthetics/trade/usePositionSellerState.ts"
      provides: "Position seller state management (close size, receive token, trigger price)"
    - path: "src/domain/synthetics/trade/utils/validation.ts"
      provides: "Decrease order validation (getDecreaseError)"
  key_links:
    - from: "src/components/PositionItem/PositionItem.tsx"
      to: "src/components/PositionSeller/PositionSeller.tsx"
      via: "onClosePositionClick -> setClosingPositionKey"
      pattern: "onClosePositionClick|closingPositionKey"
    - from: "src/components/PositionSeller/PositionSeller.tsx"
      to: "src/domain/synthetics/orders/sendBatchOrderTxn.ts"
      via: "sendBatchOrderTxn with MarketDecrease order"
      pattern: "sendBatchOrderTxn|MarketDecrease"
---

<objective>
Enable closing positions (full and partial) via the PositionSeller modal. User clicks Close on a position row, the PositionSeller modal opens showing close size input and receive token selection, and submitting creates a MarketDecrease order that the keeper executes to return collateral.

Purpose: Closing positions completes the core trading loop (open + close). Without this, users have no way to exit positions and recover their collateral.

Output: Working close position flow with full and partial close support across all 6 markets.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-position-management/06-CONTEXT.md
@.planning/phases/06-position-management/06-01-SUMMARY.md

Key context:
- PositionSeller.tsx is an inherited GMX component that handles both full and partial close.
- It uses `usePositionSellerState` for close size, receive token, and order option (Market/Limit/Trigger).
- Close orders are MarketDecrease (OrderType = 4) for market closes.
- The keeper's OrderExecutor already handles MarketDecrease orders the same as MarketIncrease.
- `selectPositionSellerDecreaseAmounts` calculates the decrease amounts, fees, and receive amount.
- The PositionSeller submits via `sendBatchOrderTxn` with a decrease order payload.
- User decision: closing supports both full close and partial close (reduce size by specific amount).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debug and fix the close position flow</name>
  <files>
    src/components/PositionSeller/PositionSeller.tsx
    src/components/PositionSeller/PositionSellerAdvancedDisplayRows.tsx
    src/domain/synthetics/trade/usePositionSellerState.ts
    src/domain/synthetics/trade/utils/validation.ts
    src/components/PositionItem/PositionItem.tsx
  </files>
  <action>
    With an open position from Plan 01, test the close position flow:

    1. **Close button wiring**: In `PositionItem.tsx`, verify the Close button calls `onClosePositionClick(position.key)` which flows to `SyntheticsPage.tsx -> setClosingPositionKey`. The `PositionSeller` component reads `closingPositionKey` via `useClosingPositionKeyState` and opens when it has a value. If the Close button is missing or hidden, check for conditional rendering that might be hiding it (e.g., `hideActions` prop or feature flags). Ensure the close button is visible on every position row.

    2. **PositionSeller modal rendering**: When `closingPositionKey` is set, `PositionSeller.tsx` should render a modal with:
       - Current position details (market, side, size, collateral, leverage)
       - Close size input (defaulting to full close, but allowing partial amounts)
       - Receive token selector
       - Fees breakdown (closing fee, price impact, borrowing fee)
       - Submit button ("Close" for market, with proper validation)

       Check that `selectPositionSellerPosition` resolves the position from the key. If it returns undefined, the position data mapping may have changed — trace from `usePositionsInfoData` through the selector.

    3. **Partial close support**: The close size input should accept any amount from 0 up to the full position size. `usePositionSellerState.ts` manages `closeSizeInputValue`. Verify:
       - Entering a value less than position size creates a partial close (reduces size by that amount)
       - Leaving at max value closes the full position
       - The remaining position values (collateral, leverage, liq price) update in real-time via `selectPositionSellerNextPositionValuesForDecrease`

    4. **Decrease validation**: `getDecreaseError` in `validation.ts` validates the close order. Check for false-positive errors:
       - Express params not required for direct wallet transactions
       - Collateral sufficiency check for partial closes (remaining collateral must be above minimum)
       - Ensure the validation allows full closes (sizeDeltaUsd === position.sizeInUsd)
       - Handle synthetic markets correctly (USDC as collateral token)

    5. **Transaction submission**: The PositionSeller calls `sendBatchOrderTxn` with a `MarketDecrease` order. Verify the batch params are constructed correctly:
       - `orderType: OrderType.MarketDecrease` (value 4)
       - `sizeDeltaUsd` matches the user's close size input
       - `initialCollateralDeltaAmount` is set for collateral withdrawal
       - Slippage/acceptable price is calculated via `selectPositionSellerMarkPrice`

       The same `sendBatchOrderTxn` from Plan 01 handles this — it's the order type that differs.

    6. **Post-close state update**: After the keeper executes the decrease order:
       - Full close: position should disappear from the positions list
       - Partial close: position should update with reduced size and collateral
       - Collateral tokens should appear in the user's wallet balance
       - SWR polling should detect the state change within 10-30 seconds
  </action>
  <verify>
    1. Click Close on an open position — PositionSeller modal opens with correct position details
    2. For full close: submit with max close size -> transaction submits, keeper executes, position disappears from list
    3. For partial close: enter half the position size -> submit, position updates with reduced size
    4. Check wallet balance increases after full close (USDC returned)
    5. Run `yarn typecheck` to ensure no TypeScript errors
    6. Test on both crypto (WETH) and synthetic (EUR) market positions
  </verify>
  <done>
    Close button on every position row opens the PositionSeller modal. Full close removes the position and returns collateral. Partial close reduces position size. Works on all 6 market types.
  </done>
</task>

</tasks>

<verification>
- Close button visible on every position row in the Positions tab
- Full close flow: modal opens, submit succeeds, position disappears, collateral returned
- Partial close flow: enter custom amount, submit succeeds, position updates with reduced size
- Works on both crypto and synthetic market positions
- No TypeScript errors (`yarn typecheck` passes)
</verification>

<success_criteria>
User can close a full or partial position on any of the 6 markets and the collateral returns to their wallet. Full close removes the position from the list. Partial close updates the position with the reduced size.
</success_criteria>

<output>
After completion, create `.planning/phases/06-position-management/06-02-SUMMARY.md`
</output>
