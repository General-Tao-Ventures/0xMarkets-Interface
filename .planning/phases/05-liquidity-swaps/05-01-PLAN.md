---
phase: 05-liquidity-swaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/GmList/GmListItem.tsx
  - src/components/StatusNotification/GmStatusNotification.tsx
  - src/domain/synthetics/markets/cancelWithdrawalTxn.ts
autonomous: true
requirements: [LIQ-01]

must_haves:
  truths:
    - "User can click 'Sell GM' on any pool from the pools list to navigate to the withdrawal form"
    - "User can submit a Sell GM transaction and see status feedback in a toast notification with elapsed time"
    - "If a withdrawal gets stuck, user sees a warning and cancel button after 120 seconds"
    - "Sell GM pre-fills with user's full GM balance (full exit only)"
  artifacts:
    - path: "src/components/GmList/GmListItem.tsx"
      provides: "Buy GM and Sell GM action buttons on each pool row"
      contains: "Buy GM"
    - path: "src/domain/synthetics/markets/cancelWithdrawalTxn.ts"
      provides: "On-chain withdrawal cancellation function"
      exports: ["cancelWithdrawalTxn"]
    - path: "src/components/StatusNotification/GmStatusNotification.tsx"
      provides: "Enhanced withdrawal notification with elapsed time, warnings, and cancel button"
      contains: "handleCancelWithdrawal"
  key_links:
    - from: "src/components/GmList/GmListItem.tsx"
      to: "/pools/details"
      via: "Button with query params setting operation=Withdrawal"
      pattern: "operation=Withdrawal"
    - from: "src/components/StatusNotification/GmStatusNotification.tsx"
      to: "src/domain/synthetics/markets/cancelWithdrawalTxn.ts"
      via: "handleCancelWithdrawal callback"
      pattern: "cancelWithdrawalTxn"
---

<objective>
Enable the complete Sell GM (withdrawal) flow: users can initiate a withdrawal from the pools list, see real-time status feedback matching the deposit UX, and cancel stuck withdrawals.

Purpose: Completes the liquidity lifecycle. Users deposited in v1.0; now they can withdraw.
Output: Working Sell GM flow with action buttons, enhanced notifications, and cancellation support.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-liquidity-swaps/05-CONTEXT.md

# Key existing files to understand
@src/components/GmList/GmListItem.tsx
@src/components/GmSwap/GmSwapBox/GmSwapBoxHeader.tsx
@src/components/StatusNotification/GmStatusNotification.tsx
@src/domain/synthetics/markets/cancelDepositTxn.ts
@src/domain/synthetics/markets/createWithdrawalTxn.ts
@src/components/GmSwap/GmSwapBox/GmDepositWithdrawalBox/useDepositWithdrawalTransactions.tsx
@src/pages/PoolsDetails/PoolsDetails.tsx
@src/context/PoolsDetailsContext/PoolsDetailsContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Buy GM and Sell GM action buttons to pool list items</name>
  <files>src/components/GmList/GmListItem.tsx</files>
  <action>
    Add "Buy GM" and "Sell GM" action buttons to each pool row in `GmListItem.tsx`, replacing or supplementing the existing "Details" button.

    **Desktop layout (non-mobile):**
    Replace the existing "Details" button cell with a cell containing two buttons side by side:
    - "Buy GM" button (green variant, `variant="primary-action"`) linking to `/pools/details?market=${marketOrGlvTokenAddress}&operation=Deposit`
    - "Sell GM" button (red-tinted, `variant="secondary"` with custom red text class) linking to `/pools/details?market=${marketOrGlvTokenAddress}&operation=Withdrawal`
    - Keep the three-dot "Details" menu icon as a small ghost button after the action buttons

    **Mobile layout:**
    Replace the existing single "View Details" button at the bottom of the mobile card with a row of two buttons:
    - "Buy GM" (green primary action) and "Sell GM" (secondary with red text)
    - Both link to the pool details page with the appropriate `operation` query param

    **Important:** The pool detail page `PoolsDetails.tsx` already reads `operation` from the `PoolsDetailsContext`. Check `src/context/PoolsDetailsContext/PoolsDetailsContext.tsx` to understand how the operation state is initialized — if it reads from URL query params, the `?operation=Withdrawal` approach works directly. If not, you may need to update `PoolsDetailsContext` to parse `operation` from the URL search params on mount and set it via `setOperation`.

    Per user decision: Sell GM is "full exit only" (no partial withdrawal). The existing `GmDepositWithdrawalBox` already handles withdrawal amounts — the "full exit" behavior will be enforced in Task 2 of this plan at the notification/UX level, and in the GmSwapBox by auto-selecting max balance. However, the buttons themselves just navigate to the page with the correct operation mode.

    Use existing `Button` component from `components/Button/Button`. Follow existing import patterns and Tailwind class conventions. Use `Trans` from `@lingui/macro` for the button text.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors. Visually inspect that `GmListItem.tsx` has both Buy GM and Sell GM buttons with correct link targets in both desktop and mobile layouts.
  </verify>
  <done>
    Each pool row in the pools list shows "Buy GM" and "Sell GM" action buttons. Clicking "Sell GM" navigates to the pool detail page with the withdrawal operation pre-selected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cancelWithdrawalTxn and enhance withdrawal notification UX</name>
  <files>
    src/domain/synthetics/markets/cancelWithdrawalTxn.ts
    src/domain/synthetics/markets/index.ts
    src/components/StatusNotification/GmStatusNotification.tsx
  </files>
  <action>
    **Part A: Create `cancelWithdrawalTxn.ts`**

    Create `src/domain/synthetics/markets/cancelWithdrawalTxn.ts` following the exact pattern of `cancelDepositTxn.ts`:
    - Import `t` from `@lingui/macro`, `Signer`/`ethers` from `ethers`, `getContract` from `config/contracts`, `callContract` from `lib/contracts`, `abis` from `sdk/abis`, `ContractsChainId` from `sdk/configs/chains`
    - Export async function `cancelWithdrawalTxn(chainId: ContractsChainId, signer: Signer, withdrawalKey: string)`
    - Create contract instance with `ExchangeRouter` ABI
    - Call `cancelWithdrawal` method with `[withdrawalKey]`
    - Use messages: sentMsg `Cancelling withdrawal.`, successMsg `Withdrawal cancelled.`, failMsg `Failed to cancel withdrawal.`

    Add the export to `src/domain/synthetics/markets/index.ts` if not already exported.

    **Part B: Enhance withdrawal notification in `GmStatusNotification.tsx`**

    The current withdrawal section in the `executionStatus` memo is minimal compared to the deposit section. Enhance it to match the deposit UX pattern:

    1. **Add elapsed time tracking for withdrawals:**
       - The existing `useDepositElapsed` hook is generic enough — use it for withdrawals too. Create a `withdrawalCreatedAt` variable from `withdrawalStatus?.createdAt` and pass it to a second `useDepositElapsed` call. If `useDepositElapsed` only supports one instance, extract the elapsed time for the withdrawal operation too. Check the hook implementation at `src/components/StatusNotification/useDepositTimeout.ts`.
       - Store the result as `withdrawalElapsedSeconds`.

    2. **Enhance the withdrawal `executionStatus` branch** to match deposit's progressive disclosure:
       - When `withdrawalStatus?.createdTxnHash` exists but no executed/cancelled:
         - `< 15s`: "Waiting for keeper to execute..."
         - `15-60s`: "Keeper executing... ({seconds}s)"
         - `60-120s`: "Taking longer than expected... ({minutes}m {seconds}s)"
         - `> 120s`: "Still waiting... ({minutes}m {seconds}s)"

    3. **Add cancel button for stuck withdrawals:**
       - Import `cancelWithdrawalTxn` from the new file
       - Add `handleCancelWithdrawal` async callback similar to `handleCancelDeposit`:
         - Guard: `if (!signer || !withdrawalStatusKey || isCancellingWithdrawal) return`
         - Set `isCancellingWithdrawal` state to true
         - Call `cancelWithdrawalTxn(chainId, signer, withdrawalStatusKey)`
         - Reset `isCancellingWithdrawal` on catch
       - Add a new `isCancellingWithdrawal` state variable
       - Render the cancel button after the executionStatus when:
         - `operation === "withdrawal"`
         - `withdrawalStatus?.createdTxnHash` exists
         - No `executedTxnHash` or `cancelledTxnHash`
         - `withdrawalElapsedSeconds >= 120`
       - Button text: "Cancel Withdrawal" (or "Cancelling..." when in progress)
       - Same styling as the deposit cancel button: `rounded bg-red-500/20 px-8 py-2 text-12 text-red-400 hover:bg-red-500/30 disabled:opacity-50`

    4. **Add warning text for withdrawals** matching deposit pattern:
       - 60-120s: "This is taking longer than usual. The keeper may be busy." (yellow)
       - 120s+: "Withdrawal may be stuck." with cancel button

    5. **Withdrawal error reason fetching** (optional enhancement):
       - When `withdrawalStatus?.cancelledTxnHash` exists, attempt to fetch error info from keeper API at `${KEEPER_API_URL}/api/withdrawals/${withdrawalStatusKey}` (if endpoint exists — the keeper may not have a withdrawal status API yet, so wrap in try/catch and gracefully degrade)
       - Create a `getWithdrawalActionableMessage` similar to the deposit one, but with withdrawal-specific messages
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors. Verify `cancelWithdrawalTxn.ts` exists and exports the function. Verify `GmStatusNotification.tsx` has the enhanced withdrawal notification code with elapsed time tracking and cancel button.
  </verify>
  <done>
    Withdrawal notifications show progressive status messages with elapsed time, display a warning after 60 seconds, and offer a cancel button after 120 seconds. The cancel button calls `cancelWithdrawalTxn` to cancel stuck withdrawals on-chain.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Pool list items show "Buy GM" and "Sell GM" buttons
3. Clicking "Sell GM" navigates to pool details with withdrawal operation selected
4. The GmSwapBox on pool detail page shows the Sell GM form when operation=Withdrawal
5. Submitting a Sell GM transaction shows enhanced notification toast with elapsed time
6. After 120s of no execution, a "Cancel Withdrawal" button appears in the toast
</verification>

<success_criteria>
- User can click "Sell GM" on any pool in the pools list page
- Pool detail page opens with Sell GM form pre-selected
- Withdrawal notification matches deposit notification UX (elapsed time, progressive warnings, cancel option)
- cancelWithdrawalTxn.ts exists and follows the cancelDepositTxn pattern
</success_criteria>

<output>
After completion, create `.planning/phases/05-liquidity-swaps/05-01-SUMMARY.md`
</output>
