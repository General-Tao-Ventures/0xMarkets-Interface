---
phase: 37-frontend-verification
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified: []
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "All pages (Trade, Pools, Dashboard/Stats, Earn) load without console errors"
    - "Trade form submits a market order and toast resolves from Pending to Executed"
    - "Deposit form submits and toast resolves from Pending to Executed"
    - "Toast notifications appear on transaction submission and update on execution"
    - "On-chain data from Plan 01 output matches what the UI displays"
  artifacts: []
  key_links: []
---

<objective>
Verify the live frontend at app.0xmarkets.io displays accurate on-chain state and all pages/forms function correctly. This is a human-verified checkpoint that uses the on-chain data output from Plan 01 as ground truth.

Purpose: Confirm the frontend is production-ready by verifying data accuracy against on-chain state and testing that all user-facing flows work end-to-end through the actual UI.

Output: Human-verified confirmation that all UI requirements pass.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-frontend-verification/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate on-chain reference data for UI comparison</name>
  <files></files>
  <action>
Run the verify-frontend-data.ts script created in Plan 01 to produce fresh on-chain reference data. Capture and format the output so the user can compare it against the live frontend.

```bash
cd e2e && npx tsx verify-frontend-data.ts 2>&1
```

Present the output clearly, organized by verification area:
1. Pool balances per market (to compare against Pools page)
2. Open positions with size/collateral/leverage (to compare against Trade page)
3. Pending orders (to compare against Trade page order list)
4. USDC and ETH balances (to compare against wallet display)

Also provide the wallet address so the user knows which account to connect.
  </action>
  <verify>
    <automated>cd e2e && npx tsx verify-frontend-data.ts 2>&1 | grep "VERIFICATION SUMMARY"</automated>
  </verify>
  <done>Fresh on-chain reference data is captured and presented to the user for comparison.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of frontend against on-chain data</name>
  <files></files>
  <action>
Present the on-chain reference data to the user and ask them to verify the live frontend at app.0xmarkets.io matches.

The user should connect the E2E test wallet and check:
- UI-01: All pages load without console errors (Trade, Pools, Stats, Accounts)
- FE-01: Pool balances on /pools match on-chain data
- FE-02: Position data on /trade matches on-chain data
- FE-03: Order status on /trade matches on-chain data
- FE-04: Token balances in wallet display match on-chain data
- UI-02: Trade form accepts input and submit button works
- UI-03: Deposit form accepts USDC input
- UI-04: Toast notifications appear on submission and resolve on execution
  </action>
  <verify>User confirms all checks pass by typing "approved"</verify>
  <done>User has verified that the frontend accurately reflects on-chain state and all UI flows work correctly. All 8 requirements (FE-01 through FE-04, UI-01 through UI-04) are confirmed.</done>
</task>

</tasks>

<verification>
All verification is human-driven in this plan. The checkpoint task covers all UI requirements.
</verification>

<success_criteria>
- All 4 pages load without console errors (UI-01)
- Pool balances in UI approximately match on-chain reference data (FE-01)
- Position data in UI matches on-chain reference data (FE-02)
- Order status in UI matches on-chain reference data (FE-03)
- Token balances in UI approximately match on-chain reference data (FE-04)
- Trade form accepts input and submit button works (UI-02)
- Deposit/withdrawal form accepts input (UI-03)
- Toast notifications appear and resolve on transaction execution (UI-04)
</success_criteria>

<output>
After completion, create `.planning/phases/37-frontend-verification/37-02-SUMMARY.md`
</output>
