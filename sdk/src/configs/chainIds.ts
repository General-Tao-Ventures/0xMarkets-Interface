// Base mainnet — wallet source, settlement, and contracts chain after mainnet cutover
export const SOURCE_BASE_MAINNET = 8453;
/** Alias for clarity when referring to Base as the contracts/settlement chain */
export const BASE_MAINNET = SOURCE_BASE_MAINNET;
// Testnet (legacy contracts / settlement; kept for local + future testnet redeploy)
export const BASE_SEPOLIA = 84532;
// Local development
export const LOCALHOST = 31337;
