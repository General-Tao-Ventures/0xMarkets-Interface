---
phase: 20-contract-address-audit
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - 0xMarkets-Interface/sdk/src/configs/markets.ts
  - 0xMarkets-Interface/sdk/src/configs/tokens.ts
  - 0xMarkets-Interface/sdk/src/configs/contracts.ts
  - 0xMarkets-Interface/src/config/static/markets.ts
  - 0xMarkets-Interface/src/config/multichain.ts
  - 0xMarkets-Interface/sdk/src/prebuilt/hashedKinkModelMarketRatesKeys.json
  - 0xMarkets-Interface/sdk/src/prebuilt/hashedMarketConfigKeys.json
  - 0xMarkets-Interface/sdk/src/prebuilt/hashedMarketValuesKeys.json
  - keeper-service/src/config/tokens.ts
  - keeper-service/.env
  - order-execution-keeper-service/.env
  - 0xMarkets-squid/src/processor.ts
  - 0xmarkets_contract/config/tokens.ts
  - docs/keeper-infrastructure.md
autonomous: false
requirements: [AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04]

must_haves:
  truths:
    - "Every market address in the interface SDK matches on-chain DataStore — no phantom markets"
    - "Every token address in the order-execution-keeper-service matches deployed token contracts"
    - "Oracle provider addresses in both keeper and interface match on-chain DataStore oracle config"
    - "All 6 markets are enabled on-chain with non-zero reserve factors, OI limits, and pool caps"
    - "A single audit report documents every discrepancy found and every fix applied"
    - "SDK prebuild succeeds after address fixes"
    - "Re-running the audit script shows zero mismatches"
    - "A deposit smoke test succeeds for all 6 markets"
  artifacts:
    - path: "0xMarkets-Interface/sdk/src/configs/markets.ts"
      provides: "Corrected market addresses matching on-chain state"
    - path: "order-execution-keeper-service/.env"
      provides: "Corrected handler and oracle addresses"
    - path: "keeper-service/src/config/tokens.ts"
      provides: "Corrected token addresses"
    - path: ".planning/phases/20-contract-address-audit/20-AUDIT-REPORT.md"
      provides: "Updated audit report with fixes applied section"
  key_links:
    - from: "0xMarkets-Interface/sdk/src/configs/markets.ts"
      to: "on-chain DataStore markets"
      via: "market token addresses as keys"
      pattern: "marketTokenAddress.*0x"
    - from: "order-execution-keeper-service/.env"
      to: "on-chain handler contracts"
      via: "DEPOSIT_HANDLER_ADDRESS, ORDER_HANDLER_ADDRESS, etc."
      pattern: "_HANDLER_ADDRESS="
    - from: "keeper-service/src/config/tokens.ts"
      to: "on-chain DataStore asset addresses"
      via: "TOKEN_ADDRESSES object"
      pattern: "TOKEN_ADDRESSES"
---

<objective>
Apply all fixes documented in the audit report from Plan 20-01, re-run the verification script to confirm zero mismatches, run SDK prebuild, and execute a smoke test (deposit per market) to prove addresses are correct at the contract level.

Purpose: This is the "second pass" of the two-pass audit approach. With the full picture from the audit report, apply all fixes in one sweep, then verify everything is correct.

Output: All config files corrected across all services, SDK prebuilt, verification script passing clean, smoke test results.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-contract-address-audit/20-CONTEXT.md
@.planning/phases/20-contract-address-audit/20-01-SUMMARY.md
@.planning/phases/20-contract-address-audit/20-AUDIT-REPORT.md
@.claude/contract-address-update-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply all address fixes documented in the audit report</name>
  <files>
    0xMarkets-Interface/sdk/src/configs/markets.ts
    0xMarkets-Interface/sdk/src/configs/tokens.ts
    0xMarkets-Interface/sdk/src/configs/contracts.ts
    0xMarkets-Interface/src/config/static/markets.ts
    0xMarkets-Interface/src/config/multichain.ts
    keeper-service/src/config/tokens.ts
    keeper-service/.env
    order-execution-keeper-service/.env
    0xMarkets-squid/src/processor.ts
    0xmarkets_contract/config/tokens.ts
    docs/keeper-infrastructure.md
  </files>
  <action>
Read the audit report at `.planning/phases/20-contract-address-audit/20-AUDIT-REPORT.md`. For every discrepancy listed in the "Discrepancy Summary" section, apply the fix:

**For each service, update addresses to match on-chain state:**

1. **Interface SDK** (`sdk/src/configs/markets.ts`, `tokens.ts`, `contracts.ts`):
   - Update any market token addresses (keys and `marketTokenAddress` fields)
   - Update any index/long/short token addresses
   - Update any infrastructure contract addresses (DataStore, ExchangeRouter, etc.)

2. **Interface UI** (`src/config/static/markets.ts`, `src/config/multichain.ts`):
   - Update market token address keys to match SDK
   - Update USDC preferred deposit token address if changed

3. **Keeper Service** (`keeper-service/src/config/tokens.ts`, `keeper-service/.env`):
   - Update TOKEN_ADDRESSES for any mismatched tokens
   - Update contract addresses in .env (READER_ADDRESS, DATA_STORE_ADDRESS, etc.)
   - Update PYTH_LAZER_FEED_PROVIDER_ADDRESS if mismatched

4. **Order Execution Keeper** (`order-execution-keeper-service/.env`):
   - Update handler addresses (DEPOSIT_HANDLER, WITHDRAWAL_HANDLER, ORDER_HANDLER, ADL_HANDLER)
   - Update PYTH_LAZER_FEED_PROVIDER_ADDRESS if mismatched
   - Update core addresses (DATA_STORE, READER, EVENT_EMITTER) if mismatched

5. **Squid Indexer** (`0xMarkets-squid/src/processor.ts`):
   - Update EVENT_EMITTER_ADDRESS if mismatched

6. **Contracts Repo** (`0xmarkets_contract/config/tokens.ts`):
   - Update baseSepolia token addresses if mismatched

7. **Docs** (`docs/keeper-infrastructure.md`):
   - Update all addresses in documentation tables to match on-chain

If the audit report shows zero discrepancies for a service, skip that service (do not make unnecessary changes).

After ALL fixes are applied, run the SDK prebuild:
```bash
cd /Users/ken/Projects/0xM/0xMarkets-Interface/sdk && yarn prebuild
```

This regenerates the prebuilt hashed keys in `sdk/src/prebuilt/`. Commit the generated files.

IMPORTANT per user decision: Aim for one commit per service repo for easy review/revert. The Interface repo gets one commit with all interface changes + prebuild output. Each external repo (keeper-service, order-execution-keeper-service, squid, contracts, docs) gets its own commit if changed.
  </action>
  <verify>
    <automated>cd /Users/ken/Projects/0xM/0xMarkets-Interface/sdk && yarn prebuild 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Every discrepancy from the audit report has been fixed
    - SDK prebuild completes successfully
    - Prebuilt JSON files are regenerated and committed
    - Changes are committed per-service (one commit per repo)
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run verification script and execute smoke test</name>
  <files>.planning/phases/20-contract-address-audit/20-AUDIT-REPORT.md</files>
  <action>
**Step 1: Re-run the audit verification script** to confirm all fixes are applied:

```bash
cd /Users/ken/Projects/0xM/0xmarkets_contract
npx hardhat run scripts/auditAddresses.ts --network baseSepolia
```

The output should show ZERO mismatches. If any remain, fix them and re-run until clean.

**Step 2: Run the smoke test** — one deposit per market across all 6 markets:

```bash
cd /Users/ken/Projects/0xM/0xmarkets_contract
TEST_MODE=deposit npx hardhat run scripts/testAllMarkets.ts --network baseSepolia
```

This submits a deposit transaction for each market (ETH, BTC, EUR, GBP, GOLD, JPY). A successful submission (TX mined, no revert) confirms the addresses are correct at the contract level.

Note: The smoke test only confirms deposit creation succeeds (addresses are valid). Keeper execution of those deposits is Phase 21's scope.

**Step 3: Update the audit report** — append a "Fixes Applied" section to `20-AUDIT-REPORT.md`:
- List every fix applied with before/after values
- Paste the clean verification script output
- Paste the smoke test results (TX hashes for all 6 markets)
- Final status: PASS or FAIL per market

If the smoke test fails for any market, document the failure in the report and investigate. A revert on deposit creation means the market or token address is still wrong — fix and re-test.
  </action>
  <verify>
    <automated>cd /Users/ken/Projects/0xM/0xmarkets_contract && npx hardhat run scripts/auditAddresses.ts --network baseSepolia 2>&1 | grep -c "MISMATCH" || echo "0 mismatches"</automated>
  </verify>
  <done>
    - Verification script shows zero mismatches across all services
    - Smoke test (deposit) submitted successfully for all 6 markets (TX mined, no revert)
    - Audit report updated with "Fixes Applied" section including verification output and smoke test TX hashes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify keeper restart and end-to-end smoke</name>
  <files>none</files>
  <action>
Human verification checkpoint. All contract addresses have been audited against on-chain DataStore state and corrected across all services:
- Interface SDK, UI config, and prebuilt keys updated
- Keeper service and order-execution-keeper-service .env files updated
- Squid, contracts repo, and docs updated
- Verification script confirms zero mismatches
- Smoke test deposits submitted for all 6 markets

Steps for the user:

1. Review the audit report at `.planning/phases/20-contract-address-audit/20-AUDIT-REPORT.md` — check the "Fixes Applied" section for completeness

2. Restart local keepers (if running):
   - Restart keeper-service (port 37017)
   - Restart order-execution-keeper-service (port 37018)
   - Confirm they start without errors

3. Deploy to cloud (DigitalOcean):
   - Pull latest changes on the DO server for both keeper repos
   - Run `docker compose up --build -d` to restart containers
   - Verify containers are running: `docker ps`

4. Verify smoke test deposits were executed by the keeper:
   - Check keeper logs for the 6 deposit TX hashes from the smoke test
   - Confirm each shows "executed" or "tx confirmed" in keeper logs

5. Quick frontend check (optional):
   - Open the app, navigate to Pools page
   - Confirm all 6 markets appear with prices
   - Confirm no blank pools or missing data
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification of keeper restart and smoke test results"</automated>
  </verify>
  <done>
    - Keepers restarted on local and cloud (DigitalOcean) with new config
    - Smoke test deposits executed by keeper (confirmed in logs)
    - Frontend shows all 6 markets with prices on Pools page
  </done>
</task>

</tasks>

<verification>
1. Re-running `auditAddresses.ts` shows zero mismatches across all services
2. SDK prebuild (`cd sdk && yarn prebuild`) completes without errors
3. Smoke test deposits submitted successfully for all 6 markets (no reverts)
4. Keeper services restarted with new config (local + cloud)
5. Audit report documents every discrepancy found and every fix applied
</verification>

<success_criteria>
- All 6 markets have correct addresses across every service (verified by automated script)
- SDK prebuilt keys are regenerated and committed
- Smoke test deposits created successfully for all 6 markets
- Keepers restarted on both local and cloud environments
- Audit report serves as a complete record of what was found and fixed
</success_criteria>

<output>
After completion, create `.planning/phases/20-contract-address-audit/20-02-SUMMARY.md`
</output>
