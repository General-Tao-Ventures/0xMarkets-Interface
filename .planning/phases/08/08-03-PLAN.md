---
phase: 08-keeper-monitoring
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - /Users/ken/Projects/0xM/order-execution-keeper-service/Dockerfile
autonomous: false
requirements: [MON-03]

must_haves:
  truths:
    - "BetterStack monitors configured for both keeper health endpoints"
    - "An alert fires within 5 minutes of either keeper going down"
    - "Both Ken and Michael receive the alert notification"
    - "order-execution-keeper-service has a Dockerfile for Docker builds"
  artifacts:
    - path: "/Users/ken/Projects/0xM/order-execution-keeper-service/Dockerfile"
      provides: "Docker build configuration for order-execution-keeper (needed for DO deployment)"
      contains: "pnpm build"
  key_links:
    - from: "BetterStack monitor"
      to: "http://142.93.203.222:37017/health"
      via: "HTTP GET every 1-2 minutes"
      pattern: "GET /health"
    - from: "BetterStack monitor"
      to: "http://142.93.203.222:37018/health"
      via: "HTTP GET every 1-2 minutes"
      pattern: "GET /health"
---

<objective>
Create a Dockerfile for the order-execution-keeper-service and set up BetterStack uptime monitoring for both keeper health endpoints.

Purpose: The order-execution-keeper needs a Dockerfile to build in Docker (the keeper-service already has one). BetterStack will ping both health endpoints every 1-2 minutes and alert Ken and Michael if either returns non-200 or times out — satisfying the "alert within 5 minutes" requirement without any keeper-side alert code.

Output: Dockerfile for order-execution-keeper + BetterStack monitors configured for both services.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08/08-CONTEXT.md
@.planning/phases/08/08-01-SUMMARY.md
@.planning/phases/08/08-02-SUMMARY.md

Reference for Dockerfile pattern:
@/Users/ken/Projects/0xM/keeper-service/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile for order-execution-keeper-service</name>
  <files>
    /Users/ken/Projects/0xM/order-execution-keeper-service/Dockerfile
  </files>
  <action>
    Create a Dockerfile at `/Users/ken/Projects/0xM/order-execution-keeper-service/Dockerfile` following the exact same pattern as the keeper-service Dockerfile at `/Users/ken/Projects/0xM/keeper-service/Dockerfile`.

    Key differences from keeper-service Dockerfile:
    - EXPOSE 37018 (not 37017)
    - Same multi-stage build: base (node:22-slim + pnpm + openssl) -> deps (install + prisma generate) -> build (tsc) -> production (copy dist + node_modules + prisma)
    - CMD: `["sh", "-c", "pnpm db:migrate:deploy && node dist/index.js"]`
    - The order-execution-keeper uses pnpm@10.22.0 (same as keeper-service)
    - Has prisma directory just like keeper-service

    The Dockerfile pattern:
    ```dockerfile
    FROM node:22-slim AS base
    RUN corepack enable && corepack prepare pnpm@10.22.0 --activate
    RUN apt-get update -y && apt-get install -y openssl && rm -rf /var/lib/apt/lists/*
    WORKDIR /app

    FROM base AS deps
    COPY package.json pnpm-lock.yaml ./
    COPY prisma ./prisma/
    RUN pnpm install --frozen-lockfile
    RUN pnpm db:generate

    FROM base AS build
    COPY --from=deps /app/node_modules ./node_modules
    COPY . .
    RUN pnpm build

    FROM base AS production
    COPY --from=deps /app/node_modules ./node_modules
    COPY --from=build /app/dist ./dist
    COPY --from=build /app/prisma ./prisma
    COPY package.json ./
    EXPOSE 37018
    CMD ["sh", "-c", "pnpm db:migrate:deploy && node dist/index.js"]
    ```

    Also check if `prisma.config.ts` needs to be copied (it exists in the order-execution-keeper root). If prisma commands reference it, add `COPY prisma.config.ts ./` in the deps stage before `RUN pnpm db:generate`.
  </action>
  <verify>
    The Dockerfile exists at `/Users/ken/Projects/0xM/order-execution-keeper-service/Dockerfile` and follows the multi-stage build pattern.
    Optionally test build locally: `cd /Users/ken/Projects/0xM/order-execution-keeper-service && docker build -t order-keeper-test .` (only if Docker is available locally).
  </verify>
  <done>
    - Dockerfile created with multi-stage build (base -> deps -> build -> production)
    - Exposes port 37018
    - Runs prisma migrate deploy + node dist/index.js on start
    - Follows same pattern as keeper-service Dockerfile
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set up BetterStack uptime monitoring</name>
  <files>None — external service configuration only</files>
  <action>
    HUMAN ACTION REQUIRED: Claude cannot create external service accounts.

    The user must create a BetterStack account and configure uptime monitors for both keeper health endpoints. Detailed instructions are provided below for the user to follow.

    **Step 1: Create BetterStack Account**
    1. Go to https://betterstack.com
    2. Sign up for the free tier (Uptime plan)
    3. No credit card required for free tier

    **Step 2: Create Monitor for keeper-service (port 37017)**
    1. In BetterStack dashboard, go to Monitors -> Create Monitor
    2. URL: `http://142.93.203.222:37017/health`
    3. Monitor type: HTTP(s)
    4. Check frequency: Every 1 minute (or 2 minutes on free tier)
    5. Confirmation period: 2 consecutive failures before alerting
    6. Expected status code: 200
    7. Name: "keeper-service (price feeds)"

    **Step 3: Create Monitor for order-execution-keeper (port 37018)**
    1. Create another monitor
    2. URL: `http://142.93.203.222:37018/health`
    3. Same settings as above
    4. Name: "order-execution-keeper (executions)"

    **Step 4: Configure Alert Channel**
    1. Go to Integrations or On-call settings
    2. Add email alerts for both Ken and Michael
    3. Or set up a Slack webhook if preferred
    4. Ensure both team members receive alerts

    **Step 5: Verify Monitors**
    1. Both monitors should show "Up" status (green) if keepers are running
    2. If monitors show "Down", ensure the DO server keepers are rebuilt with the new health endpoints from plans 08-01 and 08-02
    3. Test alert: temporarily stop one keeper container on DO (`docker compose stop keeper-service`) and wait 2-5 minutes for alert to fire, then restart (`docker compose start keeper-service`)

    **Note on deployment:** The health endpoints from plans 08-01 and 08-02 need to be deployed to the DO server before these monitors will return meaningful results. The deployment process is:
    1. SSH to 142.93.203.222
    2. `cd /opt/0xmarkets`
    3. `git pull` in each service directory (or scp updated code)
    4. `docker compose up --build -d`
  </action>
  <verify>
    User confirms: "monitoring configured" — indicating BetterStack monitors are set up for both endpoints and alert channels are configured.
  </verify>
  <done>
    - BetterStack account created with free tier
    - Two monitors configured: keeper-service (37017/health) and order-execution-keeper (37018/health)
    - Alert channel configured for both Ken and Michael
    - Monitors show "Up" status when keepers are running
  </done>
</task>

</tasks>

<verification>
1. Dockerfile exists at `/Users/ken/Projects/0xM/order-execution-keeper-service/Dockerfile`
2. BetterStack monitors configured for both `http://142.93.203.222:37017/health` and `http://142.93.203.222:37018/health`
3. Alert notifications reach both Ken and Michael
4. Monitors confirm health endpoints return 200 when keepers are running
</verification>

<success_criteria>
- order-execution-keeper-service has a Dockerfile matching keeper-service pattern
- BetterStack monitors ping both health endpoints every 1-2 minutes
- Alerts fire within 5 minutes of keeper going down (2 consecutive fails at 1-2 min interval)
- Both team members receive alert notifications
</success_criteria>

<output>
After completion, create `.planning/phases/08/08-03-SUMMARY.md`
</output>
