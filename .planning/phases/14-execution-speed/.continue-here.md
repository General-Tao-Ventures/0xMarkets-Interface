---
phase: 14-execution-speed
task: FX oracle provider diagnosis
total_tasks: 5
status: blocked
last_updated: 2026-02-25T04:00:00.000Z
---

<current_state>
Diagnosed the full chain of why EUR/USD deposits fail. Two issues found and one fixed this session.

**Issue 1 (FIXED):** Hermes provider (0x8250) was not globally enabled in DataStore.
- `isOracleProviderEnabled(0x8250)` was false
- Fixed via `scripts/enable-hermes-provider.ts`
- TX confirmed in block 38111939

**Issue 2 (ROOT CAUSE — UNSOLVED):** The address 0x8250 (Pyth contract) does NOT implement `IOracleProvider.getOraclePrice()`.
- The contracts repo has NO `PythHermesFeedProvider.sol`
- Available oracle providers: PythLazerFeedProvider, ChainlinkPriceFeedProvider, GmOracleProvider
- When the contract calls `IOracleProvider(0x8250).getOraclePrice(EUR, data)`, it reverts with "unsupported"
- This means the Hermes fallback path is fundamentally broken — there's no on-chain provider that accepts Hermes data

**Implication:** FX tokens (EUR, GBP, GOLD, JPY) cannot use Hermes as an oracle provider because no contract exists to handle Hermes price data on-chain. All tokens MUST go through the PythLazerFeedProvider.
</current_state>

<completed_work>
- Diagnosed that `isOracleProviderEnabled(Hermes)` was false → enabled it
- Confirmed mixed provider arrays ARE supported by Oracle._validatePrices() (per-token validation at line 250-280)
- Created `scripts/check-providers.ts` for verifying on-chain oracle config
- Created `scripts/enable-hermes-provider.ts` for enabling providers
- Keeper started (PID 74591, port 37018) and running with latest code
- Stale withdrawal (0xbf9fd...) now fails with "unsupported" instead of InvalidOracleProvider — confirms Hermes IS enabled but contract can't use it
</completed_work>

<remaining_work>
**Options to fix FX token oracle support (pick one):**

1. **Deploy a PythHermesFeedProvider contract** — New contract implementing IOracleProvider that:
   - Accepts Pyth VAA binary data in `getOraclePrice(token, data)`
   - Calls Pyth contract to verify and extract the price
   - Returns ValidatedPrice struct
   - Register it as enabled oracle provider, set as oracleProviderForToken for FX tokens
   - Requires contracts repo work + deployment

2. **Push FX prices through PythLazerFeedProvider** — If the Lazer feed provider has a generic `updatePrice` that stores any token's price:
   - Fetch FX prices from Hermes API
   - Call PythLazerFeedProvider.updatePrice() with FX token data
   - Set oracleProviderForToken for FX tokens back to Lazer provider (0x8a3e)
   - CHECK: Does PythLazerFeedProvider accept data for non-Lazer feeds? Read the contract first.

3. **Use Chainlink for FX tokens** — If ChainlinkPriceFeedProvider is deployed and FX price feeds exist on Base Sepolia:
   - Configure Chainlink feeds for EUR, GBP, GOLD, JPY
   - Register Chainlink as oracleProviderForToken for FX tokens
   - CHECK: Are Chainlink FX feeds available on Base Sepolia?

**Recommended next step:** Read `PythLazerFeedProvider.sol` to see if option 2 is viable (simplest fix, no deployment needed).
</remaining_work>

<decisions_made>
- Hermes provider enabled globally (DataStore setBool) — but this alone is insufficient
- The 0x8250 address is the raw Pyth contract, NOT an IOracleProvider implementation
- Oracle._validatePrices confirms per-token provider validation (mixed arrays supported)
- The keeper code is correct — buildOracleParams properly routes FX to Hermes — the problem is on-chain
</decisions_made>

<blockers>
- No on-chain oracle provider contract exists for Hermes price data
- FX tokens cannot be priced without either a new contract deployment or alternative approach
- Stale withdrawal 0xbf9fd... keeps retrying (noisy but not harmful)
</blockers>

<context>
The keeper is running on port 37018 (PID 74591). Background Lazer price updates are working for crypto tokens.
Log file: /private/tmp/claude-501/-Users-ken-Projects-0xM-0xMarkets-Interface/tasks/b8415f4.output

Key files to read next session:
- `/Users/ken/Projects/0xM/0xmarkets_contract/contracts/oracle/PythLazerFeedProvider.sol` — can it accept FX token prices?
- `/Users/ken/Projects/0xM/0xmarkets_contract/contracts/oracle/IOracleProvider.sol` — interface requirements
- `/Users/ken/Projects/0xM/0xmarkets_contract/contracts/oracle/ChainlinkPriceFeedProvider.sol` — alternative approach

Scripts created this session:
- `scripts/check-providers.ts` — verifies on-chain oracle config (both enabled status and per-token mappings)
- `scripts/enable-hermes-provider.ts` — enables a provider globally
- `scripts/check-nonce.ts` — debug nonce state
</context>

<next_action>
1. Read PythLazerFeedProvider.sol to check if it can store prices for ANY token (not just Lazer-subscribed ones)
2. If yes → modify keeper to push FX prices from Hermes through PythLazerFeedProvider.updatePrice()
3. If no → deploy a new PythHermesFeedProvider or check Chainlink availability
4. After fixing → restart keeper, submit EUR/USD deposit, verify execution
5. Continue UAT Tests 1-5
</next_action>
