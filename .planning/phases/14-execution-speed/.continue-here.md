---
phase: 14-execution-speed
task: UAT verification
total_tasks: 5
status: blocked
last_updated: 2026-02-25T03:44:53.654Z
---

<current_state>
Running Phase 14 UAT (verify-work). Test 1 of 5 (Flashblocks TX Confirmation Speed) found a blocker:
EUR/USD deposits fail with `InvalidOracleProvider(0x05d102a2)` because FX tokens (EUR, GBP, GOLD, JPY)
had the wrong oracle provider registered on-chain (Lazer instead of Hermes).

**Fix applied this session**: Re-registered all 4 FX tokens on-chain with Hermes provider via DataStore.setAddress().
Keeper restarted with latest code. Awaiting user to submit a NEW deposit to verify the fix works.
</current_state>

<completed_work>

- On-chain fix: Set oracleProviderForToken for EUR, GBP, GOLD, JPY to Hermes provider (0x8250f4aF4B972684F7b336503E2D6dFeDeB1487a) via DataStore
  - EUR TX: 0x153bb243a784da73438d7135dda88e8d2a21aff0fe99e4dcd6ffa2c51c655c5d
  - GBP TX: 0x750629e0439256e7bca9bf12260773345c316480e869f0cda8e697f046a44a30
  - GOLD TX: 0x4e0e565e6463b41c54e893931094cbffd8d4a65cdad9b4f562326ef592faf589
  - JPY TX: 0x0b7311430c0764082c6643d5f818dc35efe40a5364849d5c8fffe9eef5518aa0
- Verified on-chain: EUR now points to 0x8250 (Hermes), USDC still points to 0x8a3e (Lazer)
- Keeper restarted (PID 70469) with all Phase 14 code fixes:
  1. FX feeds removed from PYTH_LAZER_FEED_CONFIGS (prevents WebSocket crashes)
  2. Explicit gas: 200_000n on updatePriceOnChain (bypasses broken viem gas estimation)
  3. Per-token Hermes fallback in buildOracleParams (non-Lazer tokens route to Hermes)
- Background oracle updater confirmed working: updatePrice TXs sending and confirming on-chain
- UAT Test 1 recorded as "issue" (EUR/USD deposit takes 1m+)
</completed_work>

<remaining_work>

- **Immediate**: User needs to submit a NEW EUR/USD deposit on app.0xmarkets.io to verify the on-chain fix
- UAT Test 1: Re-verify Flashblocks TX confirmation speed (should now work for FX tokens)
- UAT Test 2: Background Oracle Update Cadence (pending)
- UAT Test 3: Per-Stage Timing in Deposit Logs (pending)
- UAT Test 4: Per-Stage Timing in Order Logs (pending)
- UAT Test 5: Hermes Fallback for Stale Prices (pending)
- After UAT: If issues found, diagnose and plan fixes per verify-work workflow
</remaining_work>

<decisions_made>

- FX tokens need Hermes provider on-chain (not Lazer) because they lack Lazer entitlement
- Used keeper wallet to write DataStore directly (has CONTROLLER role) rather than contracts repo deploy script
- Stopped keeper briefly to avoid nonce races during DataStore writes, then restarted
- The verify-oracle-providers.ts script only checks PYTH_LAZER_FEED_CONFIGS tokens — should be extended to also verify FX tokens use Hermes provider
</decisions_made>

<blockers>
- Previous deposits all FAILED in DB — user must create fresh deposit after on-chain fix
- Stale withdrawals keep retrying and failing (0xbf9fd...) — these are expired from old sessions, not harmful but noisy
- Keeper background task ID for logs: b58affe (output at /private/tmp/claude-501/-Users-ken-Projects-0xM-0xMarkets-Interface/tasks/b58affe.output)
</blockers>

<context>
The root cause chain was: FX tokens removed from Lazer config (correct) → FX tokens fall back to Hermes in keeper (correct) → but on-chain DataStore still had Lazer provider registered for FX tokens → contract rejects Hermes provider with InvalidOracleProvider.

The keeper is running on port 37018 with PID 70469. Oracle updates are flowing every ~5s for crypto tokens (USDC, WBTC, WETH). The on-chain fix should make FX market deposits/withdrawals work now.

UAT file is at .planning/phases/14-execution-speed/14-UAT.md — resume from Test 1 re-verification.
</context>

<next_action>
1. Ask user to submit a new EUR/USD deposit on app.0xmarkets.io
2. Check keeper logs: `tail -100 /private/tmp/claude-501/-Users-ken-Projects-0xM-0xMarkets-Interface/tasks/b58affe.output | grep -i deposit`
3. If keeper died, restart: `cd /Users/ken/Projects/0xM/order-execution-keeper-service && npx tsx src/index.ts`
4. Check DB: `psql -U ken -d order_execution_keeper -c 'SELECT status, "errorReason", "createdAt" FROM deposit_requests ORDER BY "createdAt" DESC LIMIT 3;'`
5. If deposit succeeds with timing data, mark Test 1 pass and continue UAT Tests 2-5
</next_action>
