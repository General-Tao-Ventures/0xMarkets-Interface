---
phase: 21-keeper-execution-fixes
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-02-27T01:20:00.000Z
---

<current_state>
Executing Phase 21 Plan 01 — redeployed PythLazerFeedProvider with v0.1.1-compatible code. New provider deployed, configured for all 7 tokens, enabled, and funded. Keeper .env updated. But deposits still revert — now with a DIFFERENT error than before (`InvalidOracleProvider` instead of `MaxPriceAgeExceeded`).
</current_state>

<completed_work>

- All previous pre-flight and diagnosis work (see prior session)
- ROOT CAUSE CONFIRMED: Deployed PythLazerFeedProvider called `verifyAndParseUpdate` which doesn't exist on v0.1.1 PythLazer verifier
- PythLazer proxy at `0xACeA761c27A909d4D3895128EBe6370FDE2dF481` owned by Pyth (`0x78357316239040e19fC823372cC179ca75e64b81`) — cannot upgrade
- NEW PythLazerFeedProvider deployed at `0xc5810FC1932e44866bD0D041FbfB08d8AC2A67d6`
  - Uses `verifyUpdate` + local `PythLazerLib.parseUpdateFromPayload` (v0.1.1 compatible)
  - All 7 tokens' ORACLE_PROVIDER_FOR_TOKEN updated in DataStore (verified with cast)
  - IS_ORACLE_PROVIDER_ENABLED set to true (verified with cast)
  - Funded with 0.001 ETH
- Keeper .env updated: `PYTH_LAZER_FEED_PROVIDER_ADDRESS="0xc5810FC1932e44866bD0D041FbfB08d8AC2A67d6"`
- Keeper restarted, confirmed startup log shows new provider address
- Test deposit submitted: tx `0xab2537253440cb97c7ab7189618bb4ad7dc9e1784d56c1e3b467639a46010e56` (block 38195017)
- NEW ERROR: `InvalidOracleProvider(0xc5810FC1932e44866bD0D041FbfB08d8AC2A67d6)` — error selector `0x05d102a2`
</completed_work>

<remaining_work>

- Debug `InvalidOracleProvider` error:
  - cast `getBool` returns true for `IS_ORACLE_PROVIDER_ENABLED` key, but Oracle contract still reverts
  - Need to run `cast run` on tx `0xab253725...` with full trace to see exactly which `getBool` call returns false
  - Possible: Key computation mismatch between my cast verification and the actual Solidity Keys.sol
  - Possible: Race condition — need to verify the enable tx block is before the deposit exec block
  - Check if the Solidity Keys.sol uses a different constant hash than what I computed
- Once InvalidOracleProvider fixed, resume the test matrix: 6 markets × 3 operations
- Task 2: Human verification checkpoint
</remaining_work>

<decisions_made>

- Chose to redeploy PythLazerFeedProvider (Option B) instead of upgrading PythLazer proxy (Option A), because the proxy is owned by Pyth Network
- Updated deployPythLazerFeedProviderV2.ts with current token addresses from Phase 20
- Used finishProviderConfig.ts (temp script) to handle nonce-conflict recovery for partial deploys
</decisions_made>

<blockers>

**NEW ERROR: InvalidOracleProvider(newProvider)**

The Oracle._validatePrices checks `dataStore.getBool(Keys.isOracleProviderEnabledKey(provider))`. My cast verification shows `true`, but the on-chain tx reverts with `InvalidOracleProvider`.

**Investigation path:**
1. Run `cast run 0xab2537253440cb97c7ab7189618bb4ad7dc9e1784d56c1e3b467639a46010e56` with FULL trace — find the exact `getBool` call and its key parameter
2. Compare that key with my cast-computed key to check for mismatch
3. Read the actual `Keys.sol` file to verify `IS_ORACLE_PROVIDER_ENABLED` constant hash
4. If key mismatch: re-run the enable with the correct key
5. If key matches but returns false: check if the enable tx was dropped/replaced during nonce conflicts

**Key addresses:**
- New PythLazerFeedProvider: `0xc5810FC1932e44866bD0D041FbfB08d8AC2A67d6`
- Old PythLazerFeedProvider: `0x81B3857cD770887fa1d839AbEa66f951ECa4206f`
- PythLazer verifier proxy: `0xACeA761c27A909d4D3895128EBe6370FDE2dF481` (v0.1.1, owned by Pyth)
- DataStore: `0xBaD049d5FedE7Bd9022F7E750B982349fE17e83E`
- PythLazerLib: `0x0E916868cC97181180846881096B3994820D9778`

**Temp scripts created:**
- `scripts/finishProviderConfig.ts` — used to recover from nonce-conflict partial deploy
- `scripts/deployPythLazerFeedProviderV2.ts` — updated with current token addresses
</blockers>

<context>
Key files:
- Keeper executor: `/Users/ken/Projects/0xM/order-execution-keeper-service/src/executor.ts`
- Keeper oracle: `/Users/ken/Projects/0xM/order-execution-keeper-service/src/oracle.ts`
- Keeper config: `/Users/ken/Projects/0xM/order-execution-keeper-service/src/config.ts`
- On-chain provider (current source): `contracts/oracle/PythLazerFeedProvider.sol`
- On-chain lib: `lib/pyth-crosschain/lazer/contracts/evm/src/PythLazerLib.sol`
- Keys.sol: `contracts/data/Keys.sol` — CHECK THIS for IS_ORACLE_PROVIDER_ENABLED constant
- Deploy script: `scripts/deployPythLazerFeedProviderV2.ts`
- Test script: `MARKET=WETH/USD TEST_MODE=deposit npx hardhat run scripts/testAllMarkets.ts --network baseSepolia`
- Decode reverts: `cast run <txhash> --rpc-url https://base-sepolia.core.chainstack.com/eb2a709e3101b602a19c3bebf81d1124`
- Keeper logs: `/tmp/keeper2.log`
</context>

<next_action>
1. Read Keys.sol to find the exact `IS_ORACLE_PROVIDER_ENABLED` bytes32 constant and compare with the hash I used
2. Run full `cast run` trace on the failed tx to find which getBool key is being checked
3. If key mismatch: re-enable with correct key via cast send or hardhat script
4. If key correct: check block ordering of enable tx vs deposit tx
5. Once enabled correctly, re-test deposit
</next_action>
